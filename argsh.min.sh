#!/usr/bin/env bash
# shellcheck disable=SC2178 disable=SC2120 disable=SC1090 disable=SC2046 disable=SC2155 disable=SC2154 disable=SC2329 disable=SC2086 disable=SC2128 disable=SC2206 disable=SC2064 disable=SC2016 disable=SC2317 disable=SC2163 disable=SC2030
set -euo pipefail; ARGSH_COMMIT_SHA="2fe0985a71aab84347af98049d071a72242c049c"; ARGSH_VERSION="v0.6.6-3-g2fe0985"
: "${ARGSH_VERSION:=unknown}";: "${ARGSH_COMMIT_SHA:=unknown}";: "${ARGSH_FIELD_WIDTH:=24}";COMMANDNAME=("$(s="${ARGSH_SOURCE:-"${0}"}"; echo "${s##*/}")");declare -ga __ARGSH_BUILTINS=(:usage :usage::help :usage::completion :usage::docgen :usage::mcp :args is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string import import::clear);declare -g __ARGSH_LIB_DIR="${BASH_SOURCE[0]%/*}";__argsh_try_builtin() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] || continue;enable -f "${_so}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] || continue;enable -f "${_so}/${_n}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;done;return 1;};if [[ -z "${ARGSH_BUILTIN+x}" ]];then declare -gi ARGSH_BUILTIN=0;if __argsh_try_builtin;then ARGSH_BUILTIN=1;fi;else declare -gi ARGSH_BUILTIN="${ARGSH_BUILTIN}";fi;if (( ARGSH_BUILTIN ));then ARGSH_BUILTIN=1;unset -f import 2>/dev/null || true;[[ "$(type -t import 2>/dev/null)" == "builtin" ]] || import() { declare -A _i; (( ${_i[${1}]:-} )) || { _i[${1}]=1; . "${BASH_SOURCE[0]%/*}/${1}.sh"; } };fi;if (( ARGSH_BUILTIN ));then unset -f is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string 2>/dev/null || true;fi;if ! (( ARGSH_BUILTIN ));then :usage::help() { local a44="${1}"; shift;local -a usage=("${@}");:usage::text "${a44}";exit 0;};fi;if ! (( ARGSH_BUILTIN ));then :usage::suggest() { local a36="${1}"; shift;local -a a12=("${@}");local a47="" a5=999;local a81 a4 a79 a53;local -a _sug_aliases=();for (( a81=0; a81 < ${#a12[@]}; a81+=2 ));do a4="${a12[a81]/:*}";a4="${a4/@*/}";a4="${a4#\#}";IFS='|' read -ra _sug_aliases <<< "${a4}";for a79 in "${_sug_aliases[@]}";do a53="$(:usage::levenshtein "${a36}" "${a79}")";if (( a53 < a5 ));then a5="${a53}";a47="${a79}";fi;done;done;if (( a5 > 0 && a5 <= 2 ));then local a13=${#a36};(( ${#a47} > a13 )) && a13=${#a47};if (( a5 * 100 <= a13 * 40 ));then echo "${a47}";fi;fi;};:usage::levenshtein() { local a79="${1}" a80="${2}";local a25=${#a79} a29=${#a80};if (( a25 == 0 )); then echo "${a29}"; return; fi;if (( a29 == 0 )); then echo "${a25}"; return; fi;local -a a61;local a82;for (( a82=0; a82 <= a29; a82++ )); do a61[a82]="${a82}"; done;local a81 a17 a50 a14;for (( a81=1; a81 <= a25; a81++ ));do a17="${a61[0]}";a61[0]="${a81}";for (( a82=1; a82 <= a29; a82++ ));do if [[ "${a79:a81-1:1}" == "${a80:a82-1:1}" ]];then a50=0;else a50=1;fi;a14=$(( a17 + a50 ));(( a61[a82] + 1 < a14 )) && a14=$(( a61[a82] + 1 ));(( a61[a82-1] + 1 < a14 )) && a14=$(( a61[a82-1] + 1 ));a17="${a61[a82]}";a61[a82]="${a14}";done;done;echo "${a61[a29]}";};:usage() { local a44="${1}"; shift;declare -p usage &>/dev/null || local -a usage=();declare -p args &>/dev/null || local -a args=();[[ $(( ${#usage[@]} % 2 )) -eq 0 ]] || :args::_error "usage must be an associative array";if [[ -z ${1:-} || ${1} == "-h" || ${1} == "--help" ]];then usage=(":usage::help" "${a44}" "${usage[@]}");return 0;fi;if ! (( ${#COMMANDNAME[@]} )) && [[ ${1:-} == "--argsh" ]];then echo "https://arg.sh ${ARGSH_COMMIT_SHA:-} ${ARGSH_VERSION:-}";exit 0;fi;local -A a39=();local -a a66=("${@}");local a67 a31="";while (( ${#a66[@]} ));do if [[ ${a66[0]:0:1} != "-" ]];then [[ -z "${a67:-}" ]] || break;a67="${a66[0]}";a66=("${a66[@]:1}");continue;fi;:args::parse_flag || break;a39["${a31}"]=1;done;:args::check_required_flags;local a56;for (( a81=0; a81 < ${#usage[@]}; a81+=2 ));do local _entry_cmd="${usage[a81]/:*}";_entry_cmd="${_entry_cmd/@*/}";IFS='|' read -ra _aliases <<< "${_entry_cmd}";for a26 in "${_aliases[@]}";do a26="${a26#\#}";[[ "${a67}" == "${a26}" ]] || continue;a31="${usage[a81]#\#}";a56="${usage[a81]/*:-}";a56="${a56#\#}";[[ "${a56}" == "${usage[a81]}" ]] || break 2;a56="${a56/|*}";a56="${a56/@*/}";break 2;done;done;if [[ -z "${a56:-}" ]];then if [[ -z "${a67:-}" ]];then usage=(":usage::help" "${a44}" "${usage[@]}");return 0;fi;local _sug;_sug="$(:usage::suggest "${a67}" "${usage[@]}")";if [[ -n "${_sug}" ]];then :args::error_usage "Invalid command: ${a67}. Did you mean '${_sug}'?";else :args::error_usage "Invalid command: ${a67}";fi;fi;local a9=0;[[ "${a31}" != *":-"* ]] || a9=1;if (( a9 ));then declare -F -- "${a56}" >/dev/null 2>&1 || { local _sug;_sug="$(:usage::suggest "${a67}" "${usage[@]}")";if [[ -n "${_sug}" ]];then :args::error_usage "Invalid command: ${a67}. Did you mean '${_sug}'?";else :args::error_usage "Invalid command: ${a67}";fi;};else local a16="${FUNCNAME[1]:-}";if [[ -n "${a16}" ]] && declare -F -- "${a16}::${a56}" >/dev/null 2>&1;then a56="${a16}::${a56}";elif [[ "${a16}" == *"::"* ]] && declare -F -- "${a16##*::}::${a56}" >/dev/null 2>&1;then a56="${a16##*::}::${a56}";elif declare -F -- "argsh::${a56}" >/dev/null 2>&1;then a56="argsh::${a56}";elif declare -F -- "${a56}" >/dev/null 2>&1;then :;else local _sug;_sug="$(:usage::suggest "${a67}" "${usage[@]}")";if [[ -n "${_sug}" ]];then :args::error_usage "Invalid command: ${a67}. Did you mean '${_sug}'?";else :args::error_usage "Invalid command: ${a67}";fi;fi;fi;local _cmd_name="${a31/[|:]*}";COMMANDNAME+=("${_cmd_name/@*/}");usage=("${a56}" "${a66[@]}");};:usage::text() { local a44="${1:-}";string::indent "${a44}";echo;echo "Usage: ${COMMANDNAME[*]} <command> [args]";[[ ${usage[0]:-} == '-' ]] || echo -e "\nAvailable Commands:";for (( a81=0; a81 < ${#usage[@]}; a81+=2 ));do [[ "${usage[a81]:0:1}" != "#" ]] || continue;[[ "${usage[a81]}" != "-" ]] || { echo;echo "${usage[a81+1]}";continue;};local _disp_name="${usage[a81]/[:|]*}";printf "  %-${ARGSH_FIELD_WIDTH}s %s\n" "${_disp_name/@*/}" "${usage[a81+1]}";done;:args::text_flags;echo;echo "Use \"${COMMANDNAME[*]} <command> --help\" for more information about a command.";};:args() { local a44="${1}"; shift;declare -p args &>/dev/null || local -a args=();[[ $(( ${#args[@]} % 2 )) -eq 0 ]] || :args::_error "args must be an associative array";if [[ ${1:-} == "-h" || ${1:-} == "--help" ]];then :args::text;exit 0;fi;local a32=0 a31="" a81 a0=1;local -A a39=();local -a a66=("${@}");while (( ${#a66[@]} ));do if [[ ${a66[0]:0:1} != "-" ]];then local a60 a45;a81="$(:args::field_positional "${a0}")" || :args::error_usage "too many arguments: ${a66[0]}";a31="${args[a81]}";a60="$(args::field_name "${a31}")";a45="$(:args::field_value "${a66[0]}")" || exit "${?}";local -n a71="${a60}";if is::array "${a60}";then (( a32 )) || { a71=();a32=1;};a71+=("${a45}");else a71="${a45}";fi;a66=("${a66[@]:1}");(( ++a0 ));continue;fi;if [[ "${a66[0]}" == "-h" || "${a66[0]}" == "--help" ]];then :args::text;exit 0;fi;:args::parse_flag || :args::error_usage "unknown flag: ${a66[0]}";a39["${a31}"]=1;done;if a81="$(:args::field_positional "${a0}")";then a31="$(args::field_name "${args[a81]}")";if is::uninitialized "${a31}" && ! is::array "${a31}";then :args::error_usage "missing required argument: ${a31}";fi;fi;:args::check_required_flags;[[ ${#a66[@]} -eq 0 ]] || :args::error_usage "too many arguments: ${a66[*]}";};:args::text() { declare -p args &>/dev/null || return 0;local -a a2=() a22=();:args::positional;string::indent "${a44}";echo;echo "Usage:";echo "  ${COMMANDNAME[*]} ${a22[*]}";(( ${#a2[@]} == 0 )) || { echo;echo "Arguments:";for a81 in "${a2[@]}";do [[ ${args[a81]} != "-" ]] || continue;a52="$( printf "   %-${ARGSH_FIELD_WIDTH}s%s" " " "${args[a81+1]}" | fmt::tty;)";printf "   %-${ARGSH_FIELD_WIDTH}s%s\n" "$(:args::fieldf "${args[a81]}")" "$(string::trim-left "${a52}")";done;};:args::text_flags;echo;};:args::text_flags() { local -a args=("${args[@]}");local -a a33=();array::contains 'help|h:+' "${args[@]}" || args+=('help|h:+' "Show this help message");:args::flags;(( ${#a33[@]} )) || return 0;[[ "${args[${a33[0]}]}" == "-" ]] || echo -e "\nOptions:";for a81 in "${a33[@]}";do [[ "${args[a81]:0:1}" != "#" ]] || continue;[[ "${args[a81]}" != "-" ]] || { echo;echo "${args[a81+1]}";continue;};:args::fieldf "${args[a81]}";{ echo -n "           ";echo -e "${args[a81+1]}\n";} | fmt::tty;done;};:args::flags() { declare -p args &>/dev/null || local -a args;declare -p a33 &>/dev/null || local -a a33;for (( a81=0; a81 < "${#args[@]}"; a81+=2 ));do if [[ ${args[a81]} == *"|"* || ${args[a81]} == '-' ]];then a33+=("${a81}");fi;done;};:args::positional() { declare -p args &>/dev/null || local -a args;declare -p a2 &>/dev/null || local -a a2;declare -p a22 &>/dev/null || local -a a22;local a71;for (( a81=0; a81 < "${#args[@]}"; a81+=2 ));do [[ ${args[a81]} != *"|"* && ${args[a81]} != '-' ]] || continue;a71="$(args::field_name "${args[a81]}")";a2+=("${a81}");if is::array "${a71}";then a22+=("...${a71}");continue;fi;if ! is::uninitialized "${a71}";then a22+=("[${a71}]");continue;fi;a22+=("<${a71}>");done;};:args::parse_flag() { declare -p a66 a31 &>/dev/null || return 1;local a55="${a66[0]/=*}";if [[ ${a55:0:2} == "--" ]];then a31="$(:args::field_lookup "${a55:2}")" || return "${?}";elif [[ ${a55:0:1} == "-" ]];then a55="${a55:0:2}";a31="$(:args::field_lookup "${a55:1}")" || return "${?}";fi;:args::field_set_flag "${a31}";};:args::check_required_flags() { declare -p a39 args &>/dev/null || return 1;local a31;local -a a28;for (( a81=0; a81 < ${#args[@]}; a81+=2 ));do a31="${args[a81]}";:args::field_attrs "${a31}";if (( a28[2] )) && ! (( a28[4] ));then local -n a71="${a28[0]}";a71=0;fi;if (( a28[6] )) && [[ -z ${a39[${args[a81]}]:-} ]];then :args::error_usage "missing required flag: ${args[a81]/|*}";fi;done;};:args::field_set_flag() { local a31="${1}";declare -p a66 a55 &>/dev/null || return 1;local -a a28;:args::field_attrs "${a31}";local -n a71="${a28[0]}";local a8 a6;if (( a28[2] ));then a8=1;if [[ ${a55:0:2} == "--" ]];then a66=("${a66[@]:1}");else a66[0]="-${a66[0]:2}";[[ ${a66[0]} != "-" ]] || a66=("${a66[@]:1}");fi;fi;[[ -n ${a8:-} ]] || { a6="${a66[0]/${a55}}";if [[ ${a6} == "" ]];then (( ${#a66[@]} > 1 )) || :args::error "missing value for flag: ${a28[0]}";a8="${a66[1]}";a66=("${a66[@]:1}");else [[ "${a6:0:1}" != "=" ]] || a6="${a6:1}";a8="${a6}";fi;a8="$(:args::field_value "${a8}")" || exit "${?}";a66=("${a66[@]:1}");};if (( a28[5] ));then a71+=("${a8}");else a71="${a8}";fi;};:args::field_value() { local a45="${1}";declare -p a31 &>/dev/null || return 1;declare -p a28 &>/dev/null || { local -a a28;:args::field_attrs "${a31}";};declare -f "to::${a28[3]}" &>/dev/null || :args::_error "unknown type: ${a28[3]}";"to::${a28[3]}" "${a45}" "${a28[0]}" || :args::error_usage "invalid type (${a28[3]}): ${a45}";};:args::field_lookup() { local a31="${1}";declare -p args &>/dev/null || return 1;for (( a81=0; a81 < ${#args[@]}; a81+=2 ));do if [[ ${args[a81]} =~ (^${a31}\||\|${a31}:|\|${a31}$) ]];then echo "${args[a81]}";return 0;fi;done;return 1;};:args::field_positional() { local a10="${1:-1}";declare -p args &>/dev/null || return 1;for (( a81=0; a81 < ${#args[@]}; a81+=2 ));do if [[ ${args[a81]} != *"|"* && ${args[a81]} != '-' ]];then if is::array "$(args::field_name "${args[a81]}")" || (( --a10 == 0 ));then echo "${a81}";return 0;fi;fi;done;return 1;};args::field_name() { local a31="${1}";local a27="${2:-1}";a31="${a31/[|:]*}";a31="${a31#\#}";if (( a27 ));then a31="${a31//-/_}";fi;echo "${a31}";};:args::field_attrs() { local a31="${1}";declare -p a28 &>/dev/null || local -a a28;a28=( "" "" 0 "" 0 0 0 0 "" );local a63="+~!";local a59="${a31#*[:]}";[ "${a59}" != "${a31}" ] || a59="";a28[0]="$(args::field_name "${a31}")";a28[8]="$(args::field_name "${a31}" 0)";[[ ${a28[0]:0:1} != "#" ]] || { a28[7]=1;};local -n a71="${a28[0]}";local -a a33;IFS='|' read -ra a33 <<< "${a31/[:]*}";[[ ${#a33[@]} -eq 1 ]] || { a28[1]="${a33[1]}";};if is::array "${a28[0]}";then a28[5]=1;! is::uninitialized "${a28[0]}" || a71=();! (( ${#a71[@]} )) || a28[4]=1;elif ! is::uninitialized "${a28[0]}";then a28[4]=1;fi;while (( ${#a59} > 0 ));do if [[ ${a59:0:1} == "+" ]];then [[ -z ${a28[3]} ]] || :args::_error "cannot have multiple types: ${a28[3]} and boolean";a28[2]=1;a59="${a59:1}";continue;fi;if [[ ${a59:0:1} == "~" ]];then ! (( a28[2] )) || :args::_error "already flagged as boolean";a59="${a59:1}";a28[3]="${a59/[$a63]*}";a59="${a59:${#a28[3]}}";continue;fi;if [[ ${a59:0:1} == "!" ]];then ! (( a28[6] )) || :args::_error "field already flagged as required";a28[6]=1;a59="${a59:1}";continue;fi;echo ":args error: unknown modifier: ${a59:0:1}" >&2;exit 2;done;if [[ -z ${a28[3]} && ${a28[2]} -eq 0 ]];then a28[3]="string";fi;};:args::fieldf() { local a31="${1}";declare -p a28 &>/dev/null || { local -a a28;:args::field_attrs "${a31}";};[[ ${a31} == *"|"* ]] || { echo "${a28[8]} ${a28[3]}";return 0;};local -n a71="${a28[0]}";a18="   ";! (( a28[6] )) || a18=" ! ";if [[ -n ${a28[1]} ]];then a18+="-${a28[1]}, --${a28[8]}";else a18+="    --${a28[8]}";fi;a18+=" ";! (( a28[5] )) || a18+="...";a18+="${a28[3]}";if (( a28[4] )) && ! (( a28[2] ));then a18+=" (default: ${a71[*]})";fi;echo "${a18}";};fi;args::run() { local a65="${1}"; shift;for (( a81=0; a81<${#}; a81++ ));do local a72="${1}"; shift;local a56="${1}"; shift;if ! (( a65 )) || (( a72 ));then "${a56}";fi;done;};array::contains() { local -r a21="${1}"; shift;for a11 in "${@}";do [[ "${a11}" != "${a21}" ]] || return 0;done;return 1;};array::join() { local -r a7="${1}"; shift;local a23;printf -v a23 "${a7}%s" "${@}";echo "${a23:${#a7}}";};array::nth() { local -n out="${1}";local -r a70="${2}";shift 2;for (( a81=1; a81<=${#}; a81++ ));do (( a81 % a70 )) || out+=("${!a81}");done;};bash::version() { local a38="${1:-4}";local a40="${2:-3}";local a41="${3:-0}";if [[ "${BASH_VERSINFO[0]}" -lt "${a38}" ]];then return 1;elif [[ "${BASH_VERSINFO[0]}" -gt "${a38}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[1]}" -lt "${a40}" ]];then return 1;elif [[ "${BASH_VERSINFO[1]}" -gt "${a40}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[2]}" -lt "${a41}" ]];then return 1;fi;return 0;};binary::exists() { local a15="${1}";command -v "${a15}" &> /dev/null || { echo "${a15} is required to run this script" >&2;return 1;};};binary::github() { local path="${1}";local a62="${2}";local a54="${3}";local a75="${4:-}";curl -Lso /dev/stdout "https://github.com/${a62}/releases/download/${a54}" | { if [[ -n "${a75}" ]];then tar -xz -C "$(dirname "${path}")" "${a75}";else tee "${path}" &> /dev/null;fi;chmod +x "${path}";};};binary::arch() { local a43="${1:-0}";local -r a46="$(uname -m)";case "${a46}" in
x86_64|amd64)if (( a43 )); then echo "64-bit"; else echo "amd64"; fi;;
armv7l)echo "arm";;
aarch64)echo "arm64";;
*)echo "${a46}";;
esac;};binary::jq() { binary::exists "jq" 2>/dev/null || { local -r latest="$(github::latest "stedolan/jq")" system="$(uname -s)";binary::github "${PATH_BIN?}/jq" "stedolan/jq" "${latest}/jq-${system,,}-$(binary::arch)";};};binary::vale() { binary::exists "vale" 2>/dev/null || { local -r latest="$(github::latest "errata-ai/vale")" system="$(uname -s)";binary::github "${PATH_BIN?}/vale" "errata-ai/vale" "${latest}/vale_${latest:1}_$(uname -s)_$(binary::arch 1).tar.gz" "vale";};};docker::user() { local a77="${1:-"$(id -u)"}";local a69="${2:-"$(id -g)"}";local a64="${3:-"$(whoami)"}";local a57="${4:-"/workspace"}";local a42="${5:-"/bin/sh"}";local a51;a51="$(pwd)";a51="${a51#"${PATH_BASE:-}"}";if [[ "${a51}" == "$(pwd)" ]];then a51="${a57}";else a51="${a57}${a51}";fi;local _passwd _group;_passwd="$(mktemp /tmp/docker_passwd.XXXXXX)";_group="$(mktemp /tmp/docker_group.XXXXXX)";echo "${a64}:x:${a77}:${a69}::${a57}:${a42}" > "${_passwd}";echo "${a64}:x:${a69}:" > "${_group}";echo "-v ${_passwd}:/etc/passwd -v ${_group}:/etc/group";echo "-u ${a77}:${a69}";echo "-v ${PATH_BASE:-"$(pwd)"}:${a57}";echo "-w ${a51}";if [[ -n "${PATH_BIN:-}" && -d "${PATH_BIN}" ]];then local _abs_bin;_abs_bin="$(realpath "${PATH_BIN}" 2>/dev/null || echo "${PATH_BIN}")";echo "-v ${_abs_bin}:/usr/local/sbin:ro";echo "-e PATH_BIN=/usr/local/sbin";fi;};error::stacktrace() { local -r a48="${1:-${?}}";if (( a48 ));then echo -e "\n\033[38;5;196m■■ Stacktrace(${a48}): \e[1m${BASH_COMMAND}\e[22m";for (( a81 = 1; a81 <= ${#FUNCNAME[@]} - 2; a81++ ));do echo -e "${a81}. ${BASH_SOURCE[a81]}:${BASH_LINENO[a81-1]} ➜ ${FUNCNAME[a81]}()";done;echo -e "\033[0m";return "${a48}";fi;};:args::_error() { echo -e "Error: ${1}" >&2;exit 2;};:args::error() { echo -e "Error: ${1}\n" >&2;echo "  Run \"${0##*/} -h\" for more information." >&2;exit 2;};:args::error_usage() { echo -e "Error: ${1}\n" >&2;echo "  Run \"${0##*/} -h\" for more information." >&2;exit 2;};fmt::tty() { local a74="${1:-"$(cat)"}";if ! command -v fmt &>/dev/null || [[ ! -t 1 ]];then echo "${a74}";return 0;fi;local a49;a49="$(tput cols)";echo "${a74}" | fmt -w "${a49}";};github::latest() { local a62="${1}";curl -fsSLI -o /dev/null -w "%{url_effective}" "https://github.com/${a62}/releases/latest" | rev | cut -d'/' -f1 | rev;};declare -gA a1=();: "${__ARGSH_LIB_DIR:=${BASH_SOURCE[0]%/*}}";import() { local a73="${1}";(( ${a1["${a73}"]:-} )) || { [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: import ${a73}" >&2;a1["${a73}"]=1;if [[ ${a73:0:1} == "@" ]];then a73="${PATH_BASE:?"PATH_BASE missing"}/${a73:1}";elif [[ ${a73:0:1} == "^" ]];then a73="${PATH_SCRIPTS:?"PATH_SCRIPTS missing"}/${a73:1}";elif [[ ${a73:0:1} == "~" ]];then local _s="${ARGSH_SOURCE:-${BASH_SOURCE[-1]}}";a73="${_s%/*}/${a73:1}";else local _s="${ARGSH_SOURCE:-${BASH_SOURCE[0]}}";a73="${_s%/*}/${a73}";fi;import::source "${a73}" || exit 1;};};import::source() { local a73="${1}";for a68 in "" ".sh" ".bash";do if [[ -f "${a73}${a68}" ]];then [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: import resolved -> ${a73}${a68}" >&2;. "${a73}${a68}";return;fi;done;echo "Library not found ${a73}" >&2;return 1;};import::clear() { a1=();};is::tty() { [[ -t 1 ]];};is::array() { local _decl;_decl="$(declare -p "${1}" 2>/dev/null)" || return 1;[[ "${_decl}" == "declare -a"* ]];};is::uninitialized() { local a78="${1}";if is::array "${a78}";then [[ $(declare -p "${a78}") =~ ^declare\ -a\ ${a78}(=\(\))?$ ]];else [[ ! ${!a78+x} ]];fi;};is::set() { ! is::uninitialized "${1}";};[[ -n "$(type -t :usage 2>/dev/null)" ]] || import args;argsh::builtin::try() { if [[ -n "${1:-}" ]];then [[ -f "${1}" ]] || return 1;enable -f "${1}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || return 1;return 0;fi;__argsh_try_builtin;};argsh::builtin::location() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] && { echo "${_so}"; return 0; };done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] && { echo "${_so}/${_n}"; return 0; };done;done;[[ -f "${HOME}/.local/lib/bash/${_n}" ]] && { echo "${HOME}/.local/lib/bash/${_n}"; return 0; };echo "not installed";return 1;};argsh::builtin::install_dir() { local _d;if [[ -n "${PATH_BIN:-}" && -d "${PATH_BIN}" && -w "${PATH_BIN}" ]];then echo "${PATH_BIN}"; return 0;fi;if [[ -n "${PATH_LIB:-}" && -d "${PATH_LIB}" && -w "${PATH_LIB}" ]];then echo "${PATH_LIB}"; return 0;fi;if [[ -n "${BASH_LOADABLES_PATH:-}" ]];then local IFS=:;for _d in ${BASH_LOADABLES_PATH};do [[ -d "${_d}" && -w "${_d}" ]] && { echo "${_d}"; return 0; };done;fi;_d="${HOME}/.local/lib/bash";mkdir -p "${_d}" 2>/dev/null || true;if [[ -d "${_d}" && -w "${_d}" ]];then echo "${_d}"; return 0;fi;return 1;};argsh::builtin::arch() { case "$(uname -m)" in
x86_64)echo "amd64";;
aarch64)echo "arm64";;
*)return 1;;
esac;};argsh::builtin::download() { local _force="${1:-0}" _dir _dest _tag _arch;if (( ! _force )) && argsh::builtin::try 2>/dev/null;then echo "argsh: builtins already installed" >&2;return 0;fi;[[ "$(uname -s)" == "Linux" ]] || { echo "argsh: builtins are only available for Linux (got $(uname -s))" >&2;return 1;};_arch="$(argsh::builtin::arch)" || { echo "argsh: unsupported architecture: $(uname -m)" >&2;echo "  Available: x86_64 (amd64), aarch64 (arm64)" >&2;return 1;};_dir="$(argsh::builtin::install_dir)" || { echo "argsh: no writable install path found for builtins" >&2;echo "  Run: argsh builtin install --path /your/writable/dir" >&2;return 1;};_dest="${_dir}/argsh.so";command -v curl &>/dev/null || { echo "argsh: curl is required to download builtins" >&2;return 1;};_tag="$(github::latest "arg-sh/argsh")" || { echo "argsh: failed to get latest release from GitHub" >&2;return 1;};local _asset="argsh-linux-${_arch}.so";local _tmp;_tmp="$(mktemp "${_dest}.download.XXXXXX")" || { echo "argsh: failed to create temporary download file in ${_dir}" >&2;return 1;};echo "argsh: downloading ${_asset} (${_tag})..." >&2;curl -fsSL -o "${_tmp}" "https://github.com/arg-sh/argsh/releases/download/${_tag}/${_asset}" || { echo "argsh: download failed" >&2;echo "  Asset ${_asset} may not exist for ${_tag}" >&2;rm -f "${_tmp}";return 1;};local _verify_err;_verify_err="$( (enable -f "${_tmp}" "${__ARGSH_BUILTINS[@]}") 2>&1 1>/dev/null;)" || { echo "argsh: downloaded file failed to load as builtin" >&2;[[ -n "${_verify_err}" ]] && echo "${_verify_err}" >&2;rm -f "${_tmp}";return 1;};local _mode="644";if [[ -f "${_dest}" ]];then _mode="$(stat -c '%a' "${_dest}" 2>/dev/null || echo 644)";fi;chmod "${_mode}" "${_tmp}" 2>/dev/null || true;mv -f "${_tmp}" "${_dest}" || { echo "argsh: failed to install to ${_dest}" >&2;rm -f "${_tmp}";return 1;};echo "argsh: installed to ${_dest}" >&2;return 0;};argsh::builtin() { case "${1:-}" in
install)shift; argsh::builtin::_install "${@}";;
update)shift; argsh::builtin::_install --force "${@}";;
status|"")local _loc _arch;_loc="$(argsh::builtin::location 2>/dev/null)" || _loc="not installed";_arch="$(argsh::builtin::arch 2>/dev/null)" || _arch="unsupported";echo "argsh builtin: ${_loc}";echo "  platform: $(uname -s | tr '[:upper:]' '[:lower:]')/${_arch}";echo "  loaded:   $(( ${ARGSH_BUILTIN:-0} )) (ARGSH_BUILTIN=${ARGSH_BUILTIN:-0})";echo "";echo "Usage: argsh builtin [install|update|status] [--force] [--path DIR]";echo "       Set ARGSH_BUILTIN_PATH env var to control builtin search path.";;
*)echo "argsh: unknown builtin subcommand: ${1}" >&2;echo "Usage: argsh builtin [install|update|status] [--force] [--path DIR]" >&2;return 1;;
esac;};argsh::builtins() { argsh::builtin "${@}"; };argsh::discover_dirs() { local -a _raw_dirs=();_search_dirs=();local _d _existing _skip _rd _re;if [[ -n "${PATH_TESTS:-}" ]];then IFS=';' read -ra _raw_dirs <<< "${PATH_TESTS}";fi;_raw_dirs+=( "${BASH_SOURCE[0]%/*}" "${PATH_BASE:-.}" "${PATH_BASE:-.}/test" "${PATH_BASE:-.}/tests" "${PATH_BASE:-.}/libraries" );for _d in "${_raw_dirs[@]}";do [[ -d "${_d}" ]] || continue;_skip=0;_rd="$(realpath "${_d}" 2>/dev/null || echo "${_d}")";for _existing in "${_search_dirs[@]}";do _re="$(realpath "${_existing}" 2>/dev/null || echo "${_existing}")";[[ "${_rd}" != "${_re}" ]] || { _skip=1; break; };done;(( _skip )) || _search_dirs+=("${_d}");done;};argsh::discover_files() { local -a _search_dirs=();argsh::discover_dirs;local _d _f _pattern;for _d in "${_search_dirs[@]}";do [[ -d "${_d}" ]] || continue;for _pattern in "${@}";do for _f in "${_d}"/${_pattern};do [[ -f "${_f}" ]] || continue;_found_files+=("${_f}");done;done;done;};argsh::status() { echo "argsh ${ARGSH_VERSION:-unknown} (${ARGSH_COMMIT_SHA:-unknown})";echo "  script: $(realpath "${BASH_SOURCE[0]}" 2>/dev/null || echo "${BASH_SOURCE[0]}")";echo "";local _loc _arch _so_status;_loc="$(argsh::builtin::location 2>/dev/null)" || _loc="not installed";_arch="$(argsh::builtin::arch 2>/dev/null)" || _arch="unsupported";if (( ${ARGSH_BUILTIN:-0} ));then _so_status="loaded";else _so_status="not loaded";fi;echo "Builtin (.so):";echo "  status:       ${_so_status}";echo "  path:         ${_loc}";echo "  architecture: $(uname -s | tr '[:upper:]' '[:lower:]')/${_arch}";echo "";echo "Shell:";echo "  bash: ${BASH_VERSION:-unknown}";echo "";echo "Features:";if (( ${ARGSH_BUILTIN:-0} ));then echo "  mcp:        available (builtin)";echo "  completion: available (builtin)";echo "  docgen:     available (builtin)";else echo "  mcp:        requires builtin";echo "  completion: requires builtin";echo "  docgen:     requires builtin";fi;echo "";local -a _found_files=();argsh::discover_files "*.bats";if (( ${#_found_files[@]} > 0 ));then echo "Tests: ${#_found_files[@]} .bats file(s)";local _f;for _f in "${_found_files[@]}";do echo "  ${_f}";done;else echo "Tests: none found";fi;local -a _cov_files=();local _d _cov_file;for _d in "${PATH_BASE:-.}" "${PATH_BASE:-.}"/*/;do _d="${_d%/}";[[ -f "${_d}/coverage.json" ]] && _cov_files+=("${_d}/coverage.json");done;if (( ${#_cov_files[@]} > 0 ));then echo "Coverage:";for _cov_file in "${_cov_files[@]}";do local _pct="?" _date="?";_pct="$(grep -o '"percent_covered"[^"]*"[^"]*"' "${_cov_file}" | tail -1)" && [[ "${_pct}" =~ \"([0-9.]+)\"$ ]] && _pct="${BASH_REMATCH[1]}";_date="$(grep -o '"date"[^"]*"[^"]*"' "${_cov_file}")" && [[ "${_date}" =~ \"([^\"]+)\"$ ]] && _date="${BASH_REMATCH[1]}";echo "  ${_cov_file##"${PATH_BASE:-.}"/}: ${_pct}% (${_date})";done;else echo "Coverage: no coverage.json found";fi;};argsh::_docker_forward() { binary::exists docker 2>/dev/null || { echo "argsh: this command requires either the tool installed locally or Docker" >&2;return 1;};local a76="";if [[ -t 0 ]];then a76="-it";elif [[ -p /dev/stdin ]];then a76="-i";fi;local -r a34="${ARGSH_DOCKER_IMAGE:-ghcr.io/arg-sh/argsh:latest}";local -a _env_candidates=();local _var _name;while IFS='=' read -r _var _;do _name="${_var#ARGSH_ENV_}";[[ -n "${_name}" ]] || continue;[[ "${_name}" =~ ^[A-Za-z_][A-Za-z0-9_]*$ ]] || continue;_env_candidates+=("${_name}=${!_var}");done < <(compgen -e ARGSH_ENV_ 2>/dev/null || :);( local -a _docker_env_flags=();local _kv;for _kv in "${_env_candidates[@]}";do if export "${_kv}" 2>/dev/null;then _docker_env_flags+=(-e "${_kv%%=*}");fi;done;docker run --rm ${a76} $(docker::user) -e "BATS_LOAD" -e "ARGSH_SOURCE" -e "PATH_TESTS" -e "GIT_COMMIT_SHA=$(git rev-parse HEAD 2>/dev/null || :)" -e "GIT_VERSION=$(git describe --tags --dirty 2>/dev/null || :)" "${_docker_env_flags[@]}" "${a34}" "${@}";);};argsh::minify() { if ! binary::exists minifier 2>/dev/null;then argsh::_docker_forward minify "${@}";return;fi;local template out="/dev/stdout";local -a files ignore_variable args=( 'files'              "Files to minify, can be a glob pattern" 'template|t:~file'   "Path to a template file to use for the minified file" 'out|o'              "Path to the output file" 'ignore-variable|i'  "Ignores specific variable names from obfuscation" );:args "Minify Bash files" "${@}";! is::uninitialized files || { :args::error_usage "No files to minify";return 1;};( local _content _tout;_content="$(mktemp)";_tout="$(mktemp)";trap "rm -f ${_content} ${_tout}" EXIT;local _f _file;local -a _glob;for _f in "${files[@]}";do if [[ -d "${_f}" ]];then _glob=("${_f}"/*.{sh,bash});else _glob=(${_f});fi;for _file in "${_glob[@]}";do [[ -e "${_file}" ]] || continue;{ cat "${_file}";echo;} >>"${_content}";done;done;local -a _iVars=();if ! is::uninitialized ignore_variable && (( ${#ignore_variable[@]} ));then _iVars=(-I "$(array::join "," "${ignore_variable[@]}")");fi;minifier -i "${_content}" -o "${_tout}" -O "${_iVars[@]}";local -r data="$(cat "${_tout}")";if [[ -z "${template:-}" ]];then echo -n "${data}" >"${out}";exit 0;fi;binary::exists envsubst 2>/dev/null || { echo "argsh: envsubst is required for -t/--template (install gettext)" >&2;exit 1;};local commit_sha="${GIT_COMMIT_SHA:-}";local version="${GIT_VERSION:-}";export data commit_sha version;envsubst '$data,$commit_sha,$version' <"${template}" >"${out}";);};argsh::lint() { if ! binary::exists shellcheck 2>/dev/null;then argsh::_docker_forward lint "${@}";return;fi;local only_argsh=0;local only_shellcheck=0;local -a files args=( 'only-argsh|:+'      "Skip shellcheck, run only argsh-lint" 'only-shellcheck|:+' "Skip argsh-lint, run only shellcheck" 'files'              "Files to lint, can be a glob pattern" );:args "Lint Bash files" "${@}";if (( only_argsh )) && (( only_shellcheck ));then echo "argsh lint: --only-argsh and --only-shellcheck are mutually exclusive" >&2;return 2;fi;if is::uninitialized files;then local -a _found_files=();argsh::discover_files "*.sh" "*.bash" "*.bats";local -a _search_dirs=();argsh::discover_dirs;local _d _f;for _d in "${_search_dirs[@]}";do [[ -d "${_d}" ]] || continue;for _f in "${_d}"/*;do [[ -f "${_f}" ]] || continue;local _basename="${_f##*/}";[[ "${_basename}" != *.* ]] || continue;local _shebang _is_shell=0;_shebang="$(head -1 "${_f}" 2>/dev/null || :)";local _interp="${_shebang#\#!}";_interp="${_interp# }";case "${_interp}" in
*/env|*/env\ *)_interp="${_interp#*env}";_interp="${_interp# }";[[ "${_interp}" != -S* ]] || { _interp="${_interp#-S}"; _interp="${_interp# }"; };;
esac;local _first="${_interp%% *}";_first="${_first##*/}";case "${_first}" in
bash|sh|argsh)_is_shell=1;;
esac;(( _is_shell )) && _found_files+=("${_f}");done;done;if (( ${#_found_files[@]} == 0 ));then echo "No files to lint (set PATH_TESTS or pass files as arguments)" >&2;return 1;fi;files=("${_found_files[@]}");fi;local _file _f;local -a _glob _expanded=();for _f in "${files[@]}";do if [[ -d "${_f}" ]];then _glob=("${_f}"/*.{sh,bash,bats});else _glob=(${_f});fi;for _file in "${_glob[@]}";do [[ -e "${_file}" ]] || continue;_expanded+=("${_file}");done;done;local _rc=0;if (( ! only_argsh ));then for _file in "${_expanded[@]}";do echo "Linting ${_file}" >&2;shellcheck "${_file}" || _rc=1;done;fi;if (( ! only_shellcheck ));then if binary::exists argsh-lint 2>/dev/null;then for _file in "${_expanded[@]}";do echo "argsh-lint ${_file}" >&2;argsh-lint "${_file}" || _rc=1;done;elif (( only_argsh ));then echo "argsh lint: argsh-lint binary not found on PATH" >&2;return 1;fi;fi;return "${_rc}";};argsh::test() { if ! binary::exists bats 2>/dev/null;then argsh::_docker_forward test "${@}";return;fi;local -a path args=( 'path'  "Path to the bats test files" );:args "Run tests" "${@}";if is::uninitialized path;then local -a _found_files=();argsh::discover_files "*.bats";if (( ${#_found_files[@]} == 0 ));then echo "No test files found (set PATH_TESTS or pass files as arguments)" >&2;return 1;fi;path=("${_found_files[@]}");fi;[[ -z "${BATS_LOAD:-}" ]] || echo "Running tests for ${BATS_LOAD}" >&2;bats "${path[@]}";};argsh::coverage() { if ! binary::exists kcov 2>/dev/null || ! binary::exists jq 2>/dev/null;then argsh::_docker_forward coverage "${@}";return;fi;local out="./coverage" min=75;local -a tests=(".") args=( 'tests'     "Path to the bats test files" 'out|o'     "Path to the output directory" 'min|:~int' "Minimum coverage required" );:args "Generate coverage report for your Bash scripts" "${@}";echo "Generating coverage report for: ${tests[*]}" >&2;kcov --clean --bash-dont-parse-binary-dir --include-pattern=.sh --exclude-pattern=tests --include-path=. "${out}" bats "${tests[@]}" >/dev/null 2>&1 || { echo "Failed to generate coverage report";echo "Run tests with 'argsh test' to see what went wrong";return 1;} >&2;cp "${out}"/bats.*/coverage.json "${out}/coverage.json";local coverage;coverage="$(jq -r '.percent_covered | tonumber | floor' "${out}/coverage.json")";echo "Coverage is ${coverage}% of required ${min}%";(( coverage >= min )) || return 1;};argsh::docs() { if ! binary::exists shdoc 2>/dev/null;then argsh::_docker_forward docs "${@}";return;fi;local in out prefix="";local -a args=( 'in'      "Path to the source files to generate documentation from, can be a glob pattern" 'out'     "Path to the output directory" 'prefix'  "Prefix for each md file" );:args "Generate documentation" "${@}";[[ -d "${out}" ]] || { :args::error_usage "out is not a directory";return 1;};local -a a3=(-o "${out}");[[ -z "${prefix}" ]] || a3+=(-p "${prefix}");shdoc "${a3[@]}" ${in};};argsh::main() { local -a usage=( '-'                          "Tools" 'minify:-argsh::minify'      "Minify Bash files" 'lint:-argsh::lint'          "Lint Bash files" 'test:-argsh::test'          "Run tests" 'coverage:-argsh::coverage'  "Generate coverage report for your Bash scripts" 'docs:-argsh::docs'          "Generate documentation" '-'                          "Runtime" 'builtin:-argsh::builtin'    "Manage native builtins (.so)" 'status:-argsh::_status_cmd' "Show argsh runtime status" );:usage "Enhance your Bash scripting by promoting structure and maintainability,"$'\n'"making it easier to write, understand,"$'\n'"and maintain even complex scripts." "${@}";"${usage[@]}";};argsh::_status_cmd() { declare -gi ARGSH_BUILTIN=0;if (( ${_argsh_no_builtin:-0} == 0 )) && declare -p __ARGSH_BUILTINS &>/dev/null;then argsh::builtin::try && ARGSH_BUILTIN=1;fi;argsh::status "${@}";};argsh::builtin::_install() { local _force=0 _dest_dir="";while [[ "${1:-}" == --* ]];do case "${1}" in
--force)_force=1; shift;;
--path)shift;if [[ -z "${1:-}" || "${1}" == --* ]];then echo "argsh: --path requires a directory argument" >&2;return 1;fi;_dest_dir="${1}"; shift;;
*)echo "argsh: unknown option: ${1}" >&2; return 1;;
esac;done;if [[ -n "${_dest_dir}" ]];then [[ -d "${_dest_dir}" && -w "${_dest_dir}" ]] || { echo "argsh: directory not writable: ${_dest_dir}" >&2;return 1;};PATH_BIN="${_dest_dir}" argsh::builtin::download "${_force}";else argsh::builtin::download "${_force}";fi;};argsh::shebang() { local -a _argsh_imports=();local _argsh_no_builtin=0;while [[ "${1:-}" == -* ]];do case "${1}" in
--help|-h)(argsh::main --help) || true;return 0;;
--import|-i)shift;[[ -n "${1:-}" ]] || { echo "argsh: --import requires an argument" >&2; return 1; };_argsh_imports+=("${1}");shift;;
--no-builtin)_argsh_no_builtin=1;shift;;
--version)echo "argsh ${ARGSH_VERSION:-unknown} (${ARGSH_COMMIT_SHA:-unknown})";return 0;;
--)shift; break;;
*)break;;
esac;done;if [[ -z "${1:-}" ]];then (argsh::main --help) || true;return 0;fi;local -r a54="${1}";if [[ "${BASH_SOURCE[-1]}" != "${a54}" && ! -f "${a54}" ]];then case "${a54}" in
*/*|*.sh|*.bash)echo "argsh: file not found: ${a54}" >&2;return 1;;
esac;fi;if [[ "${BASH_SOURCE[-1]}" == "${a54}" || ! -f "${a54}" ]];then : "${ARGSH_SOURCE:=argsh}";export ARGSH_SOURCE;if [[ "${a54}" == "builtins" ]];then shift;argsh::builtin "${@}";return;fi;argsh::main "${@}";return;fi;: "${ARGSH_SOURCE="${a54}"}";export ARGSH_SOURCE;bash::version 4 3 0 || { echo "This script requires bash 4.3.0 or later";return 1;} >&2;declare -gi ARGSH_BUILTIN=0;if (( ! _argsh_no_builtin ));then [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: searching for argsh.so..." >&2;if argsh::builtin::try;then ARGSH_BUILTIN=1;[[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: loaded builtins from $(argsh::builtin::location 2>/dev/null || echo 'unknown')" >&2;else [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: builtins not found locally" >&2;if [[ "${ARGSH_NO_AUTO_DOWNLOAD:-}" != "1" ]];then [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: attempting auto-download of builtins" >&2;argsh::builtin::download 0 && argsh::builtin::try && ARGSH_BUILTIN=1;else [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: auto-download disabled (ARGSH_NO_AUTO_DOWNLOAD=1)" >&2;fi;fi;fi;local _lib;for _lib in "${_argsh_imports[@]}";do import "${_lib}";done;shift;. "${a54}";};string::drop-index() { local a24="${1}";local a35="${2}";local a20="${3:-1}";echo "${a24:0:a35}${a24:a35+a20}";};string::random() { local a20="${1:-42}";local a30="${2:-"a-zA-Z0-9"}";local a74;until [[ "${a74:-}" =~ ^[[:alpha:]] ]];do a74=$(tr -dc "${a30}" < /dev/urandom | fold -w "${a20}" | head -n 1 || :);done;echo "${a74}";};string::indent() { local a24="${1:-'-'}";local a19="${2:-0}";local a58 a37;[[ ${a24} != '-' ]] || a24="$(cat)";mapfile -t a37 < <(echo "${a24}");for a58 in "${a37[@]}";do a58="$(string::trim-left "${a58}")";(( a19 == 0 )) || printf "%${a19}s" " ";echo "${a58}";done;};string::trim-left() { local a24="${1}";local a30="${2:-" "$'\n'$'\t'}";[[ -n ${a24:-} ]] || return 0;[[ ${a24} != '-' ]] || a24="$(cat)";while [[ -n "${a24}" ]];do [[ ${a30} == *"${a24:0:1}"* ]] || break;a24="${a24:1}";done;echo "${a24}";};string::trim-right() { local a24="${1:-'-'}";local a30="${2:-" "$'\n'$'\t'}";[[ ${a24} != '-' ]] || a24="$(cat)";while [[ -n "${a24}" ]];do [[ ${a30} == *"${a24: -1}"* ]] || break;a24="${a24:0: -1}";done;echo "${a24}";};string::trim() { local a24="${1:-'-'}";local a30="${2:-" "$'\n'$'\t'}";[[ ${a24} != '-' ]] || a24="$(cat)";echo "${a24}" | string::trim-left - "${a30}" | string::trim-right - "${a30}";};to::string() { local a45="${1}";echo "${a45}";};to::boolean() { local a45="${1}";case "${a45}" in
""|"false"|"0")a45="0";;
*)a45="1";;
esac;echo "${a45}";};to::int() { local a45="${1}";[[ ${a45} =~ ^-?[0-9]+$ ]] || return 1;echo "${a45}";};to::float() { local a45="${1}";[[ ${a45} =~ ^-?[0-9]+(\.[0-9]+)?$ ]] || return 1;echo "${a45}";};to::stdin() { local a45="${1}";[[ ${a45} != "-" ]] || a45="$(cat)";echo "${a45}";};to::file() { local a45="${1}";[[ -f "${a45}" ]] || return 1;echo "${a45}";};[[ "${BASH_SOURCE[0]}" != "${0}" ]] || argsh::shebang "${@}"