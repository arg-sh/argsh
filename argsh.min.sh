#!/usr/bin/env bash
# shellcheck disable=SC2178 disable=SC2120 disable=SC1090 disable=SC2046 disable=SC2155 disable=SC2154 disable=SC2329
set -euo pipefail; ARGSH_COMMIT_SHA=""; ARGSH_VERSION=""
declare -gA import_cache=();: "${__ARGSH_LIB_DIR:=${BASH_SOURCE[0]%/*}}";import() { local a59="${1}";(( ${import_cache["${a59}"]:-} )) || { import_cache["${a59}"]=1;if [[ ${a59:0:1} == "@" ]];then a59="${PATH_BASE:?"PATH_BASE missing"}/${a59:1}";elif [[ ${a59:0:1} == "~" ]];then local _s="${ARGSH_SOURCE:-${BASH_SOURCE[-1]}}";a59="${_s%/*}/${a59:1}";else local _s="${ARGSH_SOURCE:-${BASH_SOURCE[0]}}";a59="${_s%/*}/${a59}";fi;import::source "${a59}" || exit 1;};};import::source() { local a59="${1}";for a53 in "" ".sh" ".bash";do if [[ -f "${a59}${a53}" ]];then . "${a59}${a53}";return;fi;done;echo "Library not found ${a59}" >&2;return 1;};import::clear() { import_cache=();};string::drop-index() { local string="${1}";local a24="${2}";local a12="${3:-1}";echo "${string:0:a24}${string:a24+a12}";};string::random() { local a12="${1:-42}";local a20="${2:-"a-zA-Z0-9"}";local a60;until [[ "${a60:-}" =~ ^[:alpha:] ]];do a60=$(tr -dc "${a20}" < /dev/urandom | fold -w "${a12}" | head -n 1 || :);done;echo "${a60}";};string::indent() { local string="${1:-'-'}";local a10="${2:-0}";local a43 a25;[[ ${string} != '-' ]] || string="$(cat)";mapfile -t a25 < <(echo "${string}");for a43 in "${a25[@]}";do a43="$(string::trim-left "${a43}")";(( a10 == 0 )) || printf "%${a10}s" " ";echo "${a43}";done;};string::trim-left() { local string="${1}";local a20="${2:-" "$'\n'$'\t'}";[[ -n ${string:-} ]] || return 0;[[ ${string} != '-' ]] || string="$(cat)";while [[ -n "${string}" ]];do [[ ${a20} == *${string:0:1}* ]] || break;string="${string:1}";done;echo "${string}";};string::trim-right() { local string="${1:-'-'}";local a20="${2:-" "$'\n'$'\t'}";[[ ${string} != '-' ]] || string="$(cat)";while [[ -n "${string}" ]];do [[ ${a20} == *${string: -1}* ]] || break;string="${string:0: -1}";done;echo "${string}";};string::trim() { local string="${1:-'-'}";local a20="${2:-" "$'\n'$'\t'}";[[ ${string} != '-' ]] || string="$(cat)";echo "${string}" | string::trim-left - "${a20}" | string::trim-right - "${a20}";};fmt::tty() { local a60="${1:-"$(cat)"}";if ! command -v fmt &>/dev/null || [[ ! -t 1 ]];then echo "${a60}";return 0;fi;local a36;a36="$(tput cols)";echo "${a60}" | fmt -w "${a36}";};is::tty() { [[ -t 1 ]];};is::array() { declare -p "${1}" &>/dev/null && [[ $(declare -p "${1}") == "declare -a"* ]];};is::uninitialized() { local a64="${1}";if is::array "${a64}";then [[ $(declare -p "${a64}") == "declare -a ${a64}" ]];else [[ ! ${!a64+x} ]];fi;};is::set() { ! is::uninitialized "${1}";};to::string() { local a33="${1}";echo "${a33}";};to::boolean() { local a33="${1}";case "${a33}" in
""|"false"|"0")a33="0";;
*)a33="1";;
esac;echo "${a33}";};to::int() { local a33="${1}";[[ ${a33} =~ ^-?[0-9]+$ ]] || return 1;echo "${a33}";};to::float() { local a33="${1}";[[ ${a33} =~ ^-?[0-9]+(\.[0-9]+)?$ ]] || return 1;echo "${a33}";};to::stdin() { local a33="${1}";[[ ${a33} != "-" ]] || a33="$(cat)";echo "${a33}";};to::file() { local a33="${1}";[[ -f "${a33}" ]] || return 1;echo "${a33}";};error::stacktrace() { local -r a35="${1:-${?}}";if (( a35 ));then echo -e "\n\033[38;5;196m■■ Stacktrace(${a35}): \e[1m${BASH_COMMAND}\e[22m";for a65 in $(seq 1 $((${#FUNCNAME[@]} - 2)));do echo -e "${a65}. ${BASH_SOURCE[a65]}:${BASH_LINENO[a65-1]} ➜ ${FUNCNAME[a65]}()";done;echo -e "\033[0m";return "${a35}";fi;};:args::_error() { declare -p a21 &>/dev/null || local a21="???";echo ":args error [${a21}] ➜ ${1}" >&2;exit 2;};:args::error() { declare -p a21 &>/dev/null || local a21="???";echo -e "[ ${a21/[:|]*} ] invalid argument\n➜ ${1}\n" >&2;exit 2;};:args::error_usage() { declare -p a21 &>/dev/null || local a21="???";echo -e "[ ${a21/[:|]*} ] invalid usage\n➜ ${1}\n" >&2;echo -e "Use \"${0##*/} -h\" for more information" >&2;exit 2;};array::contains() { local -r a13="${1}"; shift;for a7 in "${@}";do [[ "${a7}" != "${a13}" ]] || return 0;done;return 1;};array::join() { local -r a3="${1}"; shift;local a15;printf -v a15 "${a3}%s" "${@}";echo "${a15:${#a3}}";};array::nth() { local -n a56="${1}";local -r a55="${2}";shift 2;for (( a65=1; a65<=${#}; a65++ ));do (( a65 % a55 )) || a56+=("${!a65}");done;};: "${ARGSH_VERSION:=unknown}";: "${ARGSH_COMMIT_SHA:=unknown}";: "${ARGSH_FIELD_WIDTH:=24}";COMMANDNAME=("$(s="${ARGSH_SOURCE:-"${0}"}"; echo "${s##*/}")");declare -ga __ARGSH_BUILTINS=(:usage :args is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string import import::clear);declare -g __ARGSH_LIB_DIR="${BASH_SOURCE[0]%/*}";__argsh_try_builtin() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] || continue;enable -f "${_so}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] || continue;enable -f "${_so}/${_n}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;done;return 1;};declare -gi ARGSH_BUILTIN="${ARGSH_BUILTIN:-0}";if __argsh_try_builtin;then ARGSH_BUILTIN=1;unset -f import 2>/dev/null || true;[[ "$(type -t import 2>/dev/null)" == "builtin" ]] || import() { declare -A _i; (( ${_i[${1}]:-} )) || { _i[${1}]=1; . "${BASH_SOURCE[0]%/*}/${1}.sh"; } };fi;if (( ARGSH_BUILTIN ));then unset -f is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string 2>/dev/null || true;fi;if ! (( ARGSH_BUILTIN ));then :usage() { local a32="${1}"; shift;declare -p usage &>/dev/null || local -a usage=();declare -p args &>/dev/null || local -a args=();[[ $(( ${#usage[@]} % 2 )) -eq 0 ]] || :args::_error "usage must be an associative array";[[ $(( ${#usage[@]} % 2 )) -eq 0 ]] || :args::_error "usage must be an associative array";if [[ -z ${1:-} || ${1} == "-h" || ${1} == "--help" ]];then :usage::text "${a32}";exit 0;fi;if ! (( ${#COMMANDNAME[@]} )) && [[ ${1:-} == "--argsh" ]];then echo "https://arg.sh ${ARGSH_COMMIT_SHA:-} ${ARGSH_VERSION:-}";exit 0;fi;local -A a27=();local -a a51=("${@}");local a52 a21="";while (( ${#a51[@]} ));do if [[ ${a51[0]:0:1} != "-" ]];then [[ -z "${a52:-}" ]] || break;a52="${a51[0]}";a51=("${a51[@]:1}");continue;fi;:args::parse_flag || break;a27["${a21}"]=1;done;:args::check_required_flags;local a41;for (( a65=0; a65 < ${#usage[@]}; a65+=2 ));do for a17 in $(echo "${usage[a65]/:*}" | tr '|' "\n");do a17="${a17#\#}";[[ "${a52}" == "${a17}" ]] || continue;a21="${usage[a65]#\#}";a41="${usage[a65]/*:-}";a41="${a41#\#}";[[ "${a41}" == "${usage[a65]}" ]] || break 2;a41="${a41/|*}";break 2;done;done;[[ -n "${a41:-}" ]] || :args::error_usage "Invalid command: ${a52}";local a5=0;[[ "${a21}" != *":-"* ]] || a5=1;if (( a5 ));then declare -F -- "${a41}" >/dev/null 2>&1 || :args::error_usage "Invalid command: ${a52}";else local a8="${FUNCNAME[1]:-}";if [[ -n "${a8}" ]] && declare -F -- "${a8}::${a41}" >/dev/null 2>&1;then a41="${a8}::${a41}";elif declare -F -- "argsh::${a41}" >/dev/null 2>&1;then a41="argsh::${a41}";elif declare -F -- "${a41}" >/dev/null 2>&1;then :;else :args::error_usage "Invalid command: ${a52}";fi;fi;COMMANDNAME+=("${a21/[|:]*}");usage=("${a41}" "${a51[@]}");};:usage::text() { local a32="${1:-}";string::indent "${a32}";echo;echo "Usage: ${COMMANDNAME[*]} <command> [args]";[[ ${usage[0]:-} == '-' ]] || echo -e "\nAvailable Commands:";for (( a65=0; a65 < ${#usage[@]}; a65+=2 ));do [[ "${usage[a65]:0:1}" != "#" ]] || continue;[[ "${usage[a65]}" != "-" ]] || { echo;echo "${usage[a65+1]}";continue;};printf "  %-${ARGSH_FIELD_WIDTH}s %s\n" "${usage[a65]/[:|]*}" "${usage[a65+1]}";done;:args::text_flags;echo;echo "Use \"${COMMANDNAME[*]} <command> --help\" for more information about a command.";};:args() { local a32="${1}"; shift;declare -p args &>/dev/null || local -a args=();[[ $(( ${#args[@]} % 2 )) -eq 0 ]] || :args::_error "args must be an associative array";if [[ ${1:-} == "-h" || ${1:-} == "--help" ]];then :args::text;exit 0;fi;local a22=0 a21="" a65 a0=1;local -A a27=();local -a a51=("${@}");while (( ${#a51[@]} ));do if [[ ${a51[0]:0:1} != "-" ]];then local a45 a33;a65="$(:args::field_positional "${a0}")" || :args::error_usage "too many arguments: ${a51[0]}";a21="${args[a65]}";a45="$(args::field_name "${a21}")";a33="$(:args::field_value "${a51[0]}")" || exit "${?}";local -n a57="${a45}";if is::array "${a45}";then (( a22 )) || { a57=();a22=1;};a57+=("${a33}");else a57="${a33}";fi;a51=("${a51[@]:1}");(( ++a0 ));continue;fi;:args::parse_flag || :args::error_usage "unknown a40: ${a51[0]}";a27["${a21}"]=1;done;if a65="$(:args::field_positional "${a0}")";then a21="$(args::field_name "${args[a65]}")";if is::uninitialized "${a21}" && ! is::array "${a21}";then :args::error_usage "missing required argument: ${a21}";fi;fi;:args::check_required_flags;[[ ${#a51[@]} -eq 0 ]] || :args::error_usage "too many arguments: ${a51[*]}";};:args::text() { declare -p args &>/dev/null || return 0;local -a a1=() a14=();:args::positional;string::indent "${a32}";echo;echo "Usage:";echo "  ${COMMANDNAME[*]} ${a14[*]}";(( ${#a1[@]} == 0 )) || { echo;echo "Arguments:";for a65 in "${a1[@]}";do [[ ${args[a65]} != "-" ]] || continue;a38="$( printf "   %-${ARGSH_FIELD_WIDTH}s%s" " " "${args[a65+1]}" | fmt::tty;)";printf "   %-${ARGSH_FIELD_WIDTH}s%s\n" "$(:args::fieldf "${args[a65]}")" "$(string::trim-left "${a38}")";done;};:args::text_flags;echo;};:args::text_flags() { local -a args=("${args[@]}");local -a a23=();array::contains 'help|h:+' "${args[@]}" || args+=('help|h:+' "Show this help message");:args::flags;(( ${#a23[@]} )) || return 0;[[ "${args[${a23[0]}]}" == "-" ]] || echo -e "\nOptions:";for a65 in "${a23[@]}";do [[ "${args[a65]:0:1}" != "#" ]] || continue;[[ "${args[a65]}" != "-" ]] || { echo;echo "${args[a65+1]}";continue;};:args::fieldf "${args[a65]}";{ echo -n "           ";echo -e "${args[a65+1]}\n";} | fmt::tty;done;};:args::flags() { declare -p args &>/dev/null || local -a args;declare -p a23 &>/dev/null || local -a a23;for (( a65=0; a65 < "${#args[@]}"; a65+=2 ));do if [[ ${args[a65]} == *"|"* || ${args[a65]} == '-' ]];then a23+=("${a65}");fi;done;};:args::positional() { declare -p args &>/dev/null || local -a args;declare -p a1 &>/dev/null || local -a a1;declare -p a14 &>/dev/null || local -a a14;local a57;for (( a65=0; a65 < "${#args[@]}"; a65+=2 ));do [[ ${args[a65]} != *"|"* && ${args[a65]} != '-' ]] || continue;a57="$(args::field_name "${args[a65]}")";a1+=("${a65}");if is::array "${a57}";then a14+=("...${a57}");continue;fi;if ! is::uninitialized "${a57}";then a14+=("[${a57}]");continue;fi;a14+=("<${a57}>");done;};:args::parse_flag() { declare -p a51 a21 &>/dev/null || return 1;local a40="${a51[0]/=*}";if [[ ${a40:0:2} == "--" ]];then a21="$(:args::field_lookup "${a40:2}")" || return "${?}";elif [[ ${a40:0:1} == "-" ]];then a40="${a40:0:2}";a21="$(:args::field_lookup "${a40:1}")" || return "${?}";fi;:args::field_set_flag "${a21}";};:args::check_required_flags() { declare -p a27 args &>/dev/null || return 1;local a21;local -a a19;for (( a65=0; a65 < ${#args[@]}; a65+=2 ));do a21="${args[a65]}";:args::field_attrs "${a21}";if (( a19[2] )) && ! (( a19[4] ));then local -n a57="${a19[0]}";a57=0;fi;if (( a19[6] )) && [[ -z ${a27[${args[a65]}]:-} ]];then :args::error_usage "missing required a40: ${args[a65]/|*}";fi;done;};:args::field_set_flag() { local a21="${1}";declare -p a51 a40 &>/dev/null || return 1;local -a a19;:args::field_attrs "${a21}";local -n a57="${a19[0]}";local a4 a2;if (( a19[2] ));then a4=1;if [[ ${a40:0:2} == "--" ]];then a51=("${a51[@]:1}");else a51[0]="-${a51[0]:2}";[[ ${a51[0]} != "-" ]] || a51=("${a51[@]:1}");fi;fi;[[ -n ${a4:-} ]] || { a2="${a51[0]/${a40}}";if [[ ${a2} == "" ]];then (( ${#a51[@]} )) || :args::error "missing value for a40: ${a19[0]}";a4="${a51[1]}";a51=("${a51[@]:1}");else [[ "${a2:0:1}" != "=" ]] || a2="${a2:1}";a4="${a2}";fi;a4="$(:args::field_value "${a4}")" || exit "${?}";a51=("${a51[@]:1}");};if (( a19[5] ));then a57+=("${a4}");else a57="${a4}";fi;};:args::field_value() { local a33="${1}";declare -p a21 &>/dev/null || return 1;declare -p a19 &>/dev/null || { local -a a19;:args::field_attrs "${a21}";};declare -f "to::${a19[3]}" &>/dev/null || :args::_error "unknown type: ${a19[3]}";"to::${a19[3]}" "${a33}" "${a19[0]}" || :args::error_usage "invalid type (${a19[3]}): ${a33}";};:args::field_lookup() { local a21="${1}";declare -p args &>/dev/null || return 1;for (( a65=0; a65 < ${#args[@]}; a65+=2 ));do if [[ ${args[a65]} =~ (^${a21}\||\|${a21}:|\|${a21}$) ]];then echo "${args[a65]}";return 0;fi;done;return 1;};:args::field_positional() { local a6="${1:-1}";declare -p args &>/dev/null || return 1;for (( a65=0; a65 < ${#args[@]}; a65+=2 ));do if [[ ${args[a65]} != *"|"* && ${args[a65]} != '-' ]];then if is::array "$(args::field_name "${args[a65]}")" || (( --a6 == 0 ));then echo "${a65}";return 0;fi;fi;done;return 1;};args::field_name() { local a21="${1}";local a18="${2:-1}";a21="${a21/[|:]*}";a21="${a21#\#}";if (( a18 ));then a21="${a21//-/_}";fi;echo "${a21}";};:args::field_attrs() { local a21="${1}";declare -p a19 &>/dev/null || local -a a19;a19=( "" "" 0 "" 0 0 0 0 "" );local a48="+~!";local a44="${a21#*[:]}";[ "${a44}" != "${a21}" ] || a44="";a19[0]="$(args::field_name "${a21}")";a19[8]="$(args::field_name "${a21}" 0)";[[ ${a19[0]:0:1} != "#" ]] || { a19[7]=1;};local -n a57="${a19[0]}";local -a a23;mapfile -t a23 < <(echo "${a21/[:]*}" | tr '|' '\n');[[ ${#a23[@]} -eq 1 ]] || { a19[1]="${a23[1]}";};if is::array "${a19[0]}";then a19[5]=1;! is::uninitialized "${a19[0]}" || a57=();! (( ${#a57[@]} )) || a19[4]=1;elif ! is::uninitialized "${a19[0]}";then a19[4]=1;fi;while (( ${#a44} > 0 ));do if [[ ${a44:0:1} == "+" ]];then [[ -z ${a19[3]} ]] || :args::_error "cannot have multiple types: ${a19[3]} and boolean";a19[2]=1;a44="${a44:1}";continue;fi;if [[ ${a44:0:1} == "~" ]];then ! (( a19[2] )) || :args::_error "already flagged as boolean";a44="${a44:1}";a19[3]="${a44/[$a48]*}";a44="${a44:${#a19[3]}}";continue;fi;if [[ ${a44:0:1} == "!" ]];then ! (( a19[6] )) || :args::_error "field already flagged as required";a19[6]=1;a44="${a44:1}";continue;fi;echo ":args error: unknown modifier: ${a44:0:1}" >&2;exit 2;done;if [[ -z ${a19[3]} && ${a19[2]} -eq 0 ]];then a19[3]="string";fi;};:args::fieldf() { local a21="${1}";declare -p a19 &>/dev/null || { local -a a19;:args::field_attrs "${a21}";};[[ ${a21} == *"|"* ]] || { echo "${a19[8]} ${a19[3]}";return 0;};local -n a57="${a19[0]}";a9="   ";! (( a19[6] )) || a9=" ! ";if [[ -n ${a19[1]} ]];then a9+="-${a19[1]}, --${a19[8]}";else a9+="    --${a19[8]}";fi;a9+=" ";! (( a19[5] )) || a9+="...";a9+="${a19[3]}";if (( a19[4] )) && ! (( a19[2] ));then a9+=" (default: ${a57[*]})";fi;echo "${a9}";};fi;args::run() { local a50="${1}"; shift;for (( a65=0; a65<${#}; a65++ ));do local a58="${1}"; shift;local a41="${1}"; shift;if ! (( a50 )) || (( a58 ));then "${a41}";fi;done;};bash::version() { local a26="${1:-4}";local a28="${2:-3}";local a29="${3:-0}";if [[ "${BASH_VERSINFO[0]}" -lt "${a26}" ]];then return 1;elif [[ "${BASH_VERSINFO[0]}" -gt "${a26}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[1]}" -lt "${a28}" ]];then return 1;elif [[ "${BASH_VERSINFO[1]}" -gt "${a28}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[2]}" -lt "${a29}" ]];then return 1;fi;return 0;};binary::exists() { local binary="${1}";command -v "${binary}" &> /dev/null || { echo "${binary} is required to run this script" >&2;return 1;};};binary::github() { local a46="${1}";local -r binary="$(basename "${a46}")";local a47="${2}";local a39="${3}";local a61="${4:-}";curl -Lso /dev/stdout "https://github.com/${a47}/releases/download/${a39}" | { if [[ -n "${a61}" ]];then tar -xz -C "$(dirname "${a46}")" "${a61}";else tee "${a46}" &> /dev/null;fi;chmod +x "${a46}";};};binary::arch() { local a31="${1}";local -r a34="$(uname -m)";case "${a34}" in
x86_64|amd64)if (( a31 )); then echo "64-bit"; else echo "amd64"; fi;;
armv7l)echo "arm";;
aarch64)echo "arm64";;
*)echo "${a34}";;
esac;};binary::jq() { binary::exists "jq" 2>/dev/null || { local -r a11="$(github::latest "stedolan/jq")" a16="$(uname -s)";binary::github "${PATH_BIN?}/jq" "stedolan/jq" "${a11}/jq-${a16,,}-$(binary::arch)";};};binary::vale() { binary::exists "vale" 2>/dev/null || { local -r a11="$(github::latest "errata-ai/vale")" a16="$(uname -s)";binary::github "${PATH_BIN?}/vale" "errata-ai/vale" "${a11}/vale_${a11:1}_$(uname -s)_$(binary::arch 1).tar.gz" "vale";};};docker::user() { local a63="${1:-"$(id -u)"}";local a54="${2:-"$(id -g)"}";local a49="${3:-"$(whoami)"}";local a42="${4:-"/workspace"}";local a30="${5:-"/bin/sh"}";local a37;a37="$(pwd)";a37="${a37#"${PATH_BASE:-}"}";if [[ "${a37}" == "$(pwd)" ]];then a37="${a42}";else a37="${a42}${a37}";fi;echo "${a49}:x:${a63}:${a54}::${a42}:${a30}" > /tmp/docker_passwd;echo "${a49}:x:${a54}:" > /tmp/docker_group;echo "-v /tmp/docker_passwd:/etc/passwd -v /tmp/docker_group:/etc/group";echo "-u ${a63}:${a54}";echo "-v ${PATH_BASE:-"$(pwd)"}:${a42}";echo "-w ${a37}";};github::latest() { local a47="${1}";curl -fsSLI -o /dev/null -w "%{url_effective}" "https://github.com/${a47}/releases/latest" | rev | cut -d'/' -f1 | rev;};argsh::builtin::try() { if [[ -n "${1:-}" ]];then [[ -f "${1}" ]] || return 1;enable -f "${1}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || return 1;return 0;fi;__argsh_try_builtin;};argsh::builtin::location() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] && { echo "${_so}"; return 0; };done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] && { echo "${_so}/${_n}"; return 0; };done;done;[[ -f "${HOME}/.local/lib/bash/${_n}" ]] && { echo "${HOME}/.local/lib/bash/${_n}"; return 0; };echo "not installed";return 1;};argsh::builtin::install_dir() { local _d;if [[ -n "${PATH_BIN:-}" && -d "${PATH_BIN}" && -w "${PATH_BIN}" ]];then echo "${PATH_BIN}"; return 0;fi;if [[ -n "${PATH_LIB:-}" && -d "${PATH_LIB}" && -w "${PATH_LIB}" ]];then echo "${PATH_LIB}"; return 0;fi;if [[ -n "${BASH_LOADABLES_PATH:-}" ]];then local IFS=:;for _d in ${BASH_LOADABLES_PATH};do [[ -d "${_d}" && -w "${_d}" ]] && { echo "${_d}"; return 0; };done;fi;_d="${HOME}/.local/lib/bash";mkdir -p "${_d}" 2>/dev/null || true;if [[ -d "${_d}" && -w "${_d}" ]];then echo "${_d}"; return 0;fi;return 1;};argsh::builtin::download() { local _force="${1:-0}" _dir _dest _tag;if (( ! _force )) && argsh::builtin::try 2>/dev/null;then echo "argsh: builtins already installed" >&2;return 0;fi;[[ "$(uname -s)" == "Linux" && "$(uname -m)" == "x86_64" ]] || { echo "argsh: builtins only available for linux/amd64 (got $(uname -s)/$(uname -m))" >&2;return 1;};_dir="$(argsh::builtin::install_dir)" || { echo "argsh: no writable install path found for builtins" >&2;echo "  Run: argsh builtins install --path /your/writable/dir" >&2;return 1;};_dest="${_dir}/argsh.so";command -v curl &>/dev/null || { echo "argsh: curl is required to download builtins" >&2;return 1;};_tag="$(github::latest "arg-sh/argsh")" || { echo "argsh: failed to get latest release from GitHub" >&2;return 1;};echo "argsh: downloading argsh.so (${_tag})..." >&2;curl -fsSL -o "${_dest}" "https://github.com/arg-sh/argsh/releases/download/${_tag}/argsh.so" || { echo "argsh: download failed" >&2;rm -f "${_dest}";return 1;};echo "argsh: installed to ${_dest}" >&2;argsh::builtin::try "${_dest}" || { echo "argsh: downloaded file failed to load as builtin" >&2;rm -f "${_dest}";return 1;};return 0;};argsh::builtins() { case "${1:-}" in
install)shift; argsh::builtin::_install "${@}";;
update)shift; argsh::builtin::_install --force "${@}";;
*)local _loc;_loc="$(argsh::builtin::location 2>/dev/null)" || _loc="not installed";echo "argsh builtins: ${_loc}";echo "";echo "Usage: argsh builtins install|update [--force] [--path DIR]";;
esac;};argsh::builtin::_install() { local _force=0 _a46="";while [[ "${1:-}" == --* ]];do case "${1}" in
--force)_force=1; shift;;
--path)shift; _a46="${1}"; shift;;
*)echo "argsh: unknown option: ${1}" >&2; return 1;;
esac;done;if [[ -n "${_path}" ]];then [[ -d "${_path}" && -w "${_path}" ]] || { echo "argsh: directory not writable: ${_path}" >&2;return 1;};PATH_BIN="${_path}" argsh::builtin::download "${_force}";else argsh::builtin::download "${_force}";fi;};argsh::shebang() { local _argsh_builtin_mode="" _argsh_builtin_a46="";local -a _argsh_imports=();while [[ "${1:-}" == -* ]];do case "${1}" in
--builtin)_argsh_builtin_mode="required";shift;if [[ -n "${1:-}" && "${1:0:1}" != "-" && "${1}" == *.so ]];then _argsh_builtin_a46="${1}";shift;fi;;
--no-builtin)_argsh_builtin_mode="disabled";shift;;
--import|-i)shift;[[ -n "${1:-}" ]] || { echo "argsh: --import requires an argument" >&2; return 1; };_argsh_imports+=("${1}");shift;;
--version)echo "argsh ${ARGSH_VERSION:-unknown} (${ARGSH_COMMIT_SHA:-unknown})";return 0;;
--)shift; break;;
*)break;;
esac;done;local -r a39="${1}";: "${ARGSH_SOURCE="${a39}"}";export ARGSH_SOURCE;case "${a39}" in
builtins)shift; argsh::builtins "${@}"; return;;
esac;[[ "${BASH_SOURCE[-1]}" != "${a39}" && -f "${a39}" ]] || { binary::exists docker || { echo "This script requires Docker to be installed";return 1;} >&2;local a62="";! tty -s || a62="-it";docker run --rm ${a62} $(docker::user) -e "BATS_LOAD" -e "ARGSH_SOURCE" -e "GIT_COMMIT_SHA=$(git rev-parse HEAD 2>/dev/null || :)" -e "GIT_VERSION=$(git describe --tags --dirty 2>/dev/null || :)" ghcr.io/arg-sh/argsh:latest "${@}";return 0;};bash::version 4 3 0 || { echo "This script requires bash 4.3.0 or later";return 1;} >&2;declare -gi ARGSH_BUILTIN=0;case "${_argsh_builtin_mode}" in
required)argsh::builtin::try "${_argsh_builtin_path}" || { if [[ -n "${_argsh_builtin_path}" ]];then echo "argsh: builtin not found: ${_argsh_builtin_path}" >&2;return 1;fi;argsh::builtin::download || return 1;};ARGSH_BUILTIN=1;;
disabled);;
*)argsh::builtin::try && ARGSH_BUILTIN=1;;
esac;local _lib;for _lib in "${_argsh_imports[@]}";do import "${_lib}";done;shift;. "${a39}";};