#!/usr/bin/env bash
# shellcheck disable=SC2178 disable=SC2120 disable=SC1090 disable=SC2046 disable=SC2155 disable=SC2154 disable=SC2329 disable=SC2086 disable=SC2128 disable=SC2206 disable=SC2064 disable=SC2016 disable=SC2317
set -euo pipefail; ARGSH_COMMIT_SHA="6f3385aa761481c526b60007804999cd3fccb790"; ARGSH_VERSION="v0.6.3-7-g6f3385a"
: "${ARGSH_VERSION:=unknown}";: "${ARGSH_COMMIT_SHA:=unknown}";: "${ARGSH_FIELD_WIDTH:=24}";COMMANDNAME=("$(s="${ARGSH_SOURCE:-"${0}"}"; echo "${s##*/}")");declare -ga __ARGSH_BUILTINS=(:usage :usage::help :usage::completion :usage::docgen :usage::mcp :args is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string import import::clear);declare -g __ARGSH_LIB_DIR="${BASH_SOURCE[0]%/*}";__argsh_try_builtin() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] || continue;enable -f "${_so}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] || continue;enable -f "${_so}/${_n}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;done;return 1;};if [[ -z "${ARGSH_BUILTIN+x}" ]];then declare -gi ARGSH_BUILTIN=0;if __argsh_try_builtin;then ARGSH_BUILTIN=1;fi;else declare -gi ARGSH_BUILTIN="${ARGSH_BUILTIN}";fi;if (( ARGSH_BUILTIN ));then ARGSH_BUILTIN=1;unset -f import 2>/dev/null || true;[[ "$(type -t import 2>/dev/null)" == "builtin" ]] || import() { declare -A _i; (( ${_i[${1}]:-} )) || { _i[${1}]=1; . "${BASH_SOURCE[0]%/*}/${1}.sh"; } };fi;if (( ARGSH_BUILTIN ));then unset -f is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string 2>/dev/null || true;fi;if ! (( ARGSH_BUILTIN ));then :usage::help() { local a46="${1}"; shift;local -a usage=("${@}");:usage::text "${a46}";exit 0;};fi;if ! (( ARGSH_BUILTIN ));then :usage::suggest() { local a38="${1}"; shift;local -a a12=("${@}");local a49="" a5=999;local a83 a4 a81 a55;local -a _sug_aliases=();for (( a83=0; a83 < ${#a12[@]}; a83+=2 ));do a4="${a12[a83]/:*}";a4="${a4/@*/}";a4="${a4#\#}";IFS='|' read -ra _sug_aliases <<< "${a4}";for a81 in "${_sug_aliases[@]}";do a55="$(:usage::levenshtein "${a38}" "${a81}")";if (( a55 < a5 ));then a5="${a55}";a49="${a81}";fi;done;done;if (( a5 > 0 && a5 <= 2 ));then local a13=${#a38};(( ${#a49} > a13 )) && a13=${#a49};if (( a5 * 100 <= a13 * 40 ));then echo "${a49}";fi;fi;};:usage::levenshtein() { local a81="${1}" a82="${2}";local a27=${#a81} a31=${#a82};if (( a27 == 0 )); then echo "${a31}"; return; fi;if (( a31 == 0 )); then echo "${a27}"; return; fi;local -a a63;local a84;for (( a84=0; a84 <= a31; a84++ )); do a63[a84]="${a84}"; done;local a83 a17 a52 a14;for (( a83=1; a83 <= a27; a83++ ));do a17="${a63[0]}";a63[0]="${a83}";for (( a84=1; a84 <= a31; a84++ ));do if [[ "${a81:a83-1:1}" == "${a82:a84-1:1}" ]];then a52=0;else a52=1;fi;a14=$(( a17 + a52 ));(( a63[a84] + 1 < a14 )) && a14=$(( a63[a84] + 1 ));(( a63[a84-1] + 1 < a14 )) && a14=$(( a63[a84-1] + 1 ));a17="${a63[a84]}";a63[a84]="${a14}";done;done;echo "${a63[a31]}";};:usage() { local a46="${1}"; shift;declare -p usage &>/dev/null || local -a usage=();declare -p args &>/dev/null || local -a args=();[[ $(( ${#usage[@]} % 2 )) -eq 0 ]] || :args::_error "usage must be an associative array";if [[ -z ${1:-} || ${1} == "-h" || ${1} == "--help" ]];then usage=(":usage::help" "${a46}" "${usage[@]}");return 0;fi;if ! (( ${#COMMANDNAME[@]} )) && [[ ${1:-} == "--argsh" ]];then echo "https://arg.sh ${ARGSH_COMMIT_SHA:-} ${ARGSH_VERSION:-}";exit 0;fi;local -A a41=();local -a a68=("${@}");local a69 a33="";while (( ${#a68[@]} ));do if [[ ${a68[0]:0:1} != "-" ]];then [[ -z "${a69:-}" ]] || break;a69="${a68[0]}";a68=("${a68[@]:1}");continue;fi;:args::parse_flag || break;a41["${a33}"]=1;done;:args::check_required_flags;local a58;for (( a83=0; a83 < ${#usage[@]}; a83+=2 ));do local _entry_cmd="${usage[a83]/:*}";_entry_cmd="${_entry_cmd/@*/}";IFS='|' read -ra _aliases <<< "${_entry_cmd}";for a28 in "${_aliases[@]}";do a28="${a28#\#}";[[ "${a69}" == "${a28}" ]] || continue;a33="${usage[a83]#\#}";a58="${usage[a83]/*:-}";a58="${a58#\#}";[[ "${a58}" == "${usage[a83]}" ]] || break 2;a58="${a58/|*}";a58="${a58/@*/}";break 2;done;done;if [[ -z "${a58:-}" ]];then if [[ -z "${a69:-}" ]];then usage=(":usage::help" "${a46}" "${usage[@]}");return 0;fi;local _sug;_sug="$(:usage::suggest "${a69}" "${usage[@]}")";if [[ -n "${_sug}" ]];then :args::error_usage "Invalid command: ${a69}. Did you mean '${_sug}'?";else :args::error_usage "Invalid command: ${a69}";fi;fi;local a9=0;[[ "${a33}" != *":-"* ]] || a9=1;if (( a9 ));then declare -F -- "${a58}" >/dev/null 2>&1 || { local _sug;_sug="$(:usage::suggest "${a69}" "${usage[@]}")";if [[ -n "${_sug}" ]];then :args::error_usage "Invalid command: ${a69}. Did you mean '${_sug}'?";else :args::error_usage "Invalid command: ${a69}";fi;};else local a16="${FUNCNAME[1]:-}";if [[ -n "${a16}" ]] && declare -F -- "${a16}::${a58}" >/dev/null 2>&1;then a58="${a16}::${a58}";elif [[ "${a16}" == *"::"* ]] && declare -F -- "${a16##*::}::${a58}" >/dev/null 2>&1;then a58="${a16##*::}::${a58}";elif declare -F -- "argsh::${a58}" >/dev/null 2>&1;then a58="argsh::${a58}";elif declare -F -- "${a58}" >/dev/null 2>&1;then :;else local _sug;_sug="$(:usage::suggest "${a69}" "${usage[@]}")";if [[ -n "${_sug}" ]];then :args::error_usage "Invalid command: ${a69}. Did you mean '${_sug}'?";else :args::error_usage "Invalid command: ${a69}";fi;fi;fi;local _cmd_name="${a33/[|:]*}";COMMANDNAME+=("${_cmd_name/@*/}");usage=("${a58}" "${a68[@]}");};:usage::text() { local a46="${1:-}";string::indent "${a46}";echo;echo "Usage: ${COMMANDNAME[*]} <command> [args]";[[ ${usage[0]:-} == '-' ]] || echo -e "\nAvailable Commands:";for (( a83=0; a83 < ${#usage[@]}; a83+=2 ));do [[ "${usage[a83]:0:1}" != "#" ]] || continue;[[ "${usage[a83]}" != "-" ]] || { echo;echo "${usage[a83+1]}";continue;};local _disp_name="${usage[a83]/[:|]*}";printf "  %-${ARGSH_FIELD_WIDTH}s %s\n" "${_disp_name/@*/}" "${usage[a83+1]}";done;:args::text_flags;echo;echo "Use \"${COMMANDNAME[*]} <command> --help\" for more information about a command.";};:args() { local a46="${1}"; shift;declare -p args &>/dev/null || local -a args=();[[ $(( ${#args[@]} % 2 )) -eq 0 ]] || :args::_error "args must be an associative array";if [[ ${1:-} == "-h" || ${1:-} == "--help" ]];then :args::text;exit 0;fi;local a34=0 a33="" a83 a0=1;local -A a41=();local -a a68=("${@}");while (( ${#a68[@]} ));do if [[ ${a68[0]:0:1} != "-" ]];then local a62 a47;a83="$(:args::field_positional "${a0}")" || :args::error_usage "too many arguments: ${a68[0]}";a33="${args[a83]}";a62="$(args::field_name "${a33}")";a47="$(:args::field_value "${a68[0]}")" || exit "${?}";local -n a73="${a62}";if is::array "${a62}";then (( a34 )) || { a73=();a34=1;};a73+=("${a47}");else a73="${a47}";fi;a68=("${a68[@]:1}");(( ++a0 ));continue;fi;:args::parse_flag || :args::error_usage "unknown flag: ${a68[0]}";a41["${a33}"]=1;done;if a83="$(:args::field_positional "${a0}")";then a33="$(args::field_name "${args[a83]}")";if is::uninitialized "${a33}" && ! is::array "${a33}";then :args::error_usage "missing required argument: ${a33}";fi;fi;:args::check_required_flags;[[ ${#a68[@]} -eq 0 ]] || :args::error_usage "too many arguments: ${a68[*]}";};:args::text() { declare -p args &>/dev/null || return 0;local -a a2=() a23=();:args::positional;string::indent "${a46}";echo;echo "Usage:";echo "  ${COMMANDNAME[*]} ${a23[*]}";(( ${#a2[@]} == 0 )) || { echo;echo "Arguments:";for a83 in "${a2[@]}";do [[ ${args[a83]} != "-" ]] || continue;a54="$( printf "   %-${ARGSH_FIELD_WIDTH}s%s" " " "${args[a83+1]}" | fmt::tty;)";printf "   %-${ARGSH_FIELD_WIDTH}s%s\n" "$(:args::fieldf "${args[a83]}")" "$(string::trim-left "${a54}")";done;};:args::text_flags;echo;};:args::text_flags() { local -a args=("${args[@]}");local -a a35=();array::contains 'help|h:+' "${args[@]}" || args+=('help|h:+' "Show this help message");:args::flags;(( ${#a35[@]} )) || return 0;[[ "${args[${a35[0]}]}" == "-" ]] || echo -e "\nOptions:";for a83 in "${a35[@]}";do [[ "${args[a83]:0:1}" != "#" ]] || continue;[[ "${args[a83]}" != "-" ]] || { echo;echo "${args[a83+1]}";continue;};:args::fieldf "${args[a83]}";{ echo -n "           ";echo -e "${args[a83+1]}\n";} | fmt::tty;done;};:args::flags() { declare -p args &>/dev/null || local -a args;declare -p a35 &>/dev/null || local -a a35;for (( a83=0; a83 < "${#args[@]}"; a83+=2 ));do if [[ ${args[a83]} == *"|"* || ${args[a83]} == '-' ]];then a35+=("${a83}");fi;done;};:args::positional() { declare -p args &>/dev/null || local -a args;declare -p a2 &>/dev/null || local -a a2;declare -p a23 &>/dev/null || local -a a23;local a73;for (( a83=0; a83 < "${#args[@]}"; a83+=2 ));do [[ ${args[a83]} != *"|"* && ${args[a83]} != '-' ]] || continue;a73="$(args::field_name "${args[a83]}")";a2+=("${a83}");if is::array "${a73}";then a23+=("...${a73}");continue;fi;if ! is::uninitialized "${a73}";then a23+=("[${a73}]");continue;fi;a23+=("<${a73}>");done;};:args::parse_flag() { declare -p a68 a33 &>/dev/null || return 1;local a57="${a68[0]/=*}";if [[ ${a57:0:2} == "--" ]];then a33="$(:args::field_lookup "${a57:2}")" || return "${?}";elif [[ ${a57:0:1} == "-" ]];then a57="${a57:0:2}";a33="$(:args::field_lookup "${a57:1}")" || return "${?}";fi;:args::field_set_flag "${a33}";};:args::check_required_flags() { declare -p a41 args &>/dev/null || return 1;local a33;local -a a30;for (( a83=0; a83 < ${#args[@]}; a83+=2 ));do a33="${args[a83]}";:args::field_attrs "${a33}";if (( a30[2] )) && ! (( a30[4] ));then local -n a73="${a30[0]}";a73=0;fi;if (( a30[6] )) && [[ -z ${a41[${args[a83]}]:-} ]];then :args::error_usage "missing required flag: ${args[a83]/|*}";fi;done;};:args::field_set_flag() { local a33="${1}";declare -p a68 a57 &>/dev/null || return 1;local -a a30;:args::field_attrs "${a33}";local -n a73="${a30[0]}";local a8 a6;if (( a30[2] ));then a8=1;if [[ ${a57:0:2} == "--" ]];then a68=("${a68[@]:1}");else a68[0]="-${a68[0]:2}";[[ ${a68[0]} != "-" ]] || a68=("${a68[@]:1}");fi;fi;[[ -n ${a8:-} ]] || { a6="${a68[0]/${a57}}";if [[ ${a6} == "" ]];then (( ${#a68[@]} > 1 )) || :args::error "missing value for flag: ${a30[0]}";a8="${a68[1]}";a68=("${a68[@]:1}");else [[ "${a6:0:1}" != "=" ]] || a6="${a6:1}";a8="${a6}";fi;a8="$(:args::field_value "${a8}")" || exit "${?}";a68=("${a68[@]:1}");};if (( a30[5] ));then a73+=("${a8}");else a73="${a8}";fi;};:args::field_value() { local a47="${1}";declare -p a33 &>/dev/null || return 1;declare -p a30 &>/dev/null || { local -a a30;:args::field_attrs "${a33}";};declare -f "to::${a30[3]}" &>/dev/null || :args::_error "unknown type: ${a30[3]}";"to::${a30[3]}" "${a47}" "${a30[0]}" || :args::error_usage "invalid type (${a30[3]}): ${a47}";};:args::field_lookup() { local a33="${1}";declare -p args &>/dev/null || return 1;for (( a83=0; a83 < ${#args[@]}; a83+=2 ));do if [[ ${args[a83]} =~ (^${a33}\||\|${a33}:|\|${a33}$) ]];then echo "${args[a83]}";return 0;fi;done;return 1;};:args::field_positional() { local a10="${1:-1}";declare -p args &>/dev/null || return 1;for (( a83=0; a83 < ${#args[@]}; a83+=2 ));do if [[ ${args[a83]} != *"|"* && ${args[a83]} != '-' ]];then if is::array "$(args::field_name "${args[a83]}")" || (( --a10 == 0 ));then echo "${a83}";return 0;fi;fi;done;return 1;};args::field_name() { local a33="${1}";local a29="${2:-1}";a33="${a33/[|:]*}";a33="${a33#\#}";if (( a29 ));then a33="${a33//-/_}";fi;echo "${a33}";};:args::field_attrs() { local a33="${1}";declare -p a30 &>/dev/null || local -a a30;a30=( "" "" 0 "" 0 0 0 0 "" );local a65="+~!";local a61="${a33#*[:]}";[ "${a61}" != "${a33}" ] || a61="";a30[0]="$(args::field_name "${a33}")";a30[8]="$(args::field_name "${a33}" 0)";[[ ${a30[0]:0:1} != "#" ]] || { a30[7]=1;};local -n a73="${a30[0]}";local -a a35;IFS='|' read -ra a35 <<< "${a33/[:]*}";[[ ${#a35[@]} -eq 1 ]] || { a30[1]="${a35[1]}";};if is::array "${a30[0]}";then a30[5]=1;! is::uninitialized "${a30[0]}" || a73=();! (( ${#a73[@]} )) || a30[4]=1;elif ! is::uninitialized "${a30[0]}";then a30[4]=1;fi;while (( ${#a61} > 0 ));do if [[ ${a61:0:1} == "+" ]];then [[ -z ${a30[3]} ]] || :args::_error "cannot have multiple types: ${a30[3]} and boolean";a30[2]=1;a61="${a61:1}";continue;fi;if [[ ${a61:0:1} == "~" ]];then ! (( a30[2] )) || :args::_error "already flagged as boolean";a61="${a61:1}";a30[3]="${a61/[$a65]*}";a61="${a61:${#a30[3]}}";continue;fi;if [[ ${a61:0:1} == "!" ]];then ! (( a30[6] )) || :args::_error "field already flagged as required";a30[6]=1;a61="${a61:1}";continue;fi;echo ":args error: unknown modifier: ${a61:0:1}" >&2;exit 2;done;if [[ -z ${a30[3]} && ${a30[2]} -eq 0 ]];then a30[3]="string";fi;};:args::fieldf() { local a33="${1}";declare -p a30 &>/dev/null || { local -a a30;:args::field_attrs "${a33}";};[[ ${a33} == *"|"* ]] || { echo "${a30[8]} ${a30[3]}";return 0;};local -n a73="${a30[0]}";a18="   ";! (( a30[6] )) || a18=" ! ";if [[ -n ${a30[1]} ]];then a18+="-${a30[1]}, --${a30[8]}";else a18+="    --${a30[8]}";fi;a18+=" ";! (( a30[5] )) || a18+="...";a18+="${a30[3]}";if (( a30[4] )) && ! (( a30[2] ));then a18+=" (default: ${a73[*]})";fi;echo "${a18}";};fi;args::run() { local a67="${1}"; shift;for (( a83=0; a83<${#}; a83++ ));do local a74="${1}"; shift;local a58="${1}"; shift;if ! (( a67 )) || (( a74 ));then "${a58}";fi;done;};array::contains() { local -r a22="${1}"; shift;for a11 in "${@}";do [[ "${a11}" != "${a22}" ]] || return 0;done;return 1;};array::join() { local -r a7="${1}"; shift;local a24;printf -v a24 "${a7}%s" "${@}";echo "${a24:${#a7}}";};array::nth() { local -n out="${1}";local -r a72="${2}";shift 2;for (( a83=1; a83<=${#}; a83++ ));do (( a83 % a72 )) || out+=("${!a83}");done;};bash::version() { local a40="${1:-4}";local a42="${2:-3}";local a43="${3:-0}";if [[ "${BASH_VERSINFO[0]}" -lt "${a40}" ]];then return 1;elif [[ "${BASH_VERSINFO[0]}" -gt "${a40}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[1]}" -lt "${a42}" ]];then return 1;elif [[ "${BASH_VERSINFO[1]}" -gt "${a42}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[2]}" -lt "${a43}" ]];then return 1;fi;return 0;};binary::exists() { local a15="${1}";command -v "${a15}" &> /dev/null || { echo "${a15} is required to run this script" >&2;return 1;};};binary::github() { local path="${1}";local a64="${2}";local a56="${3}";local a77="${4:-}";curl -Lso /dev/stdout "https://github.com/${a64}/releases/download/${a56}" | { if [[ -n "${a77}" ]];then tar -xz -C "$(dirname "${path}")" "${a77}";else tee "${path}" &> /dev/null;fi;chmod +x "${path}";};};binary::arch() { local a45="${1:-0}";local -r a48="$(uname -m)";case "${a48}" in
x86_64|amd64)if (( a45 )); then echo "64-bit"; else echo "amd64"; fi;;
armv7l)echo "arm";;
aarch64)echo "arm64";;
*)echo "${a48}";;
esac;};binary::jq() { binary::exists "jq" 2>/dev/null || { local -r a20="$(github::latest "stedolan/jq")" a26="$(uname -s)";binary::github "${PATH_BIN?}/jq" "stedolan/jq" "${a20}/jq-${a26,,}-$(binary::arch)";};};binary::vale() { binary::exists "vale" 2>/dev/null || { local -r a20="$(github::latest "errata-ai/vale")" a26="$(uname -s)";binary::github "${PATH_BIN?}/vale" "errata-ai/vale" "${a20}/vale_${a20:1}_$(uname -s)_$(binary::arch 1).tar.gz" "vale";};};docker::user() { local a79="${1:-"$(id -u)"}";local a71="${2:-"$(id -g)"}";local a66="${3:-"$(whoami)"}";local a59="${4:-"/workspace"}";local a44="${5:-"/bin/sh"}";local a53;a53="$(pwd)";a53="${a53#"${PATH_BASE:-}"}";if [[ "${a53}" == "$(pwd)" ]];then a53="${a59}";else a53="${a59}${a53}";fi;local _passwd _group;_passwd="$(mktemp /tmp/docker_passwd.XXXXXX)";_group="$(mktemp /tmp/docker_group.XXXXXX)";echo "${a66}:x:${a79}:${a71}::${a59}:${a44}" > "${_passwd}";echo "${a66}:x:${a71}:" > "${_group}";echo "-v ${_passwd}:/etc/passwd -v ${_group}:/etc/group";echo "-u ${a79}:${a71}";echo "-v ${PATH_BASE:-"$(pwd)"}:${a59}";echo "-w ${a53}";};error::stacktrace() { local -r a50="${1:-${?}}";if (( a50 ));then echo -e "\n\033[38;5;196m■■ Stacktrace(${a50}): \e[1m${BASH_COMMAND}\e[22m";for (( a83 = 1; a83 <= ${#FUNCNAME[@]} - 2; a83++ ));do echo -e "${a83}. ${BASH_SOURCE[a83]}:${BASH_LINENO[a83-1]} ➜ ${FUNCNAME[a83]}()";done;echo -e "\033[0m";return "${a50}";fi;};:args::_error() { echo -e "Error: ${1}" >&2;exit 2;};:args::error() { echo -e "Error: ${1}\n" >&2;echo "  Run \"${0##*/} -h\" for more information." >&2;exit 2;};:args::error_usage() { echo -e "Error: ${1}\n" >&2;echo "  Run \"${0##*/} -h\" for more information." >&2;exit 2;};fmt::tty() { local a76="${1:-"$(cat)"}";if ! command -v fmt &>/dev/null || [[ ! -t 1 ]];then echo "${a76}";return 0;fi;local a51;a51="$(tput cols)";echo "${a76}" | fmt -w "${a51}";};github::latest() { local a64="${1}";curl -fsSLI -o /dev/null -w "%{url_effective}" "https://github.com/${a64}/releases/latest" | rev | cut -d'/' -f1 | rev;};declare -gA a1=();: "${__ARGSH_LIB_DIR:=${BASH_SOURCE[0]%/*}}";import() { local a75="${1}";(( ${a1["${a75}"]:-} )) || { [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: import ${a75}" >&2;a1["${a75}"]=1;if [[ ${a75:0:1} == "@" ]];then a75="${PATH_BASE:?"PATH_BASE missing"}/${a75:1}";elif [[ ${a75:0:1} == "^" ]];then a75="${PATH_SCRIPTS:?"PATH_SCRIPTS missing"}/${a75:1}";elif [[ ${a75:0:1} == "~" ]];then local _s="${ARGSH_SOURCE:-${BASH_SOURCE[-1]}}";a75="${_s%/*}/${a75:1}";else local _s="${ARGSH_SOURCE:-${BASH_SOURCE[0]}}";a75="${_s%/*}/${a75}";fi;import::source "${a75}" || exit 1;};};import::source() { local a75="${1}";for a70 in "" ".sh" ".bash";do if [[ -f "${a75}${a70}" ]];then [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: import resolved -> ${a75}${a70}" >&2;. "${a75}${a70}";return;fi;done;echo "Library not found ${a75}" >&2;return 1;};import::clear() { a1=();};is::tty() { [[ -t 1 ]];};is::array() { local _decl;_decl="$(declare -p "${1}" 2>/dev/null)" || return 1;[[ "${_decl}" == "declare -a"* ]];};is::uninitialized() { local a80="${1}";if is::array "${a80}";then [[ $(declare -p "${a80}") =~ ^declare\ -a\ ${a80}(=\(\))?$ ]];else [[ ! ${!a80+x} ]];fi;};is::set() { ! is::uninitialized "${1}";};[[ -n "$(type -t :usage 2>/dev/null)" ]] || import args;argsh::builtin::try() { if [[ -n "${1:-}" ]];then [[ -f "${1}" ]] || return 1;enable -f "${1}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || return 1;return 0;fi;__argsh_try_builtin;};argsh::builtin::location() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] && { echo "${_so}"; return 0; };done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] && { echo "${_so}/${_n}"; return 0; };done;done;[[ -f "${HOME}/.local/lib/bash/${_n}" ]] && { echo "${HOME}/.local/lib/bash/${_n}"; return 0; };echo "not installed";return 1;};argsh::builtin::install_dir() { local _d;if [[ -n "${PATH_BIN:-}" && -d "${PATH_BIN}" && -w "${PATH_BIN}" ]];then echo "${PATH_BIN}"; return 0;fi;if [[ -n "${PATH_LIB:-}" && -d "${PATH_LIB}" && -w "${PATH_LIB}" ]];then echo "${PATH_LIB}"; return 0;fi;if [[ -n "${BASH_LOADABLES_PATH:-}" ]];then local IFS=:;for _d in ${BASH_LOADABLES_PATH};do [[ -d "${_d}" && -w "${_d}" ]] && { echo "${_d}"; return 0; };done;fi;_d="${HOME}/.local/lib/bash";mkdir -p "${_d}" 2>/dev/null || true;if [[ -d "${_d}" && -w "${_d}" ]];then echo "${_d}"; return 0;fi;return 1;};argsh::builtin::arch() { case "$(uname -m)" in
x86_64)echo "amd64";;
aarch64)echo "arm64";;
*)return 1;;
esac;};argsh::builtin::download() { local _force="${1:-0}" _dir _dest _tag _arch;if (( ! _force )) && argsh::builtin::try 2>/dev/null;then echo "argsh: builtins already installed" >&2;return 0;fi;[[ "$(uname -s)" == "Linux" ]] || { echo "argsh: builtins are only available for Linux (got $(uname -s))" >&2;return 1;};_arch="$(argsh::builtin::arch)" || { echo "argsh: unsupported architecture: $(uname -m)" >&2;echo "  Available: x86_64 (amd64), aarch64 (arm64)" >&2;return 1;};_dir="$(argsh::builtin::install_dir)" || { echo "argsh: no writable install path found for builtins" >&2;echo "  Run: argsh builtin install --path /your/writable/dir" >&2;return 1;};_dest="${_dir}/argsh.so";command -v curl &>/dev/null || { echo "argsh: curl is required to download builtins" >&2;return 1;};_tag="$(github::latest "arg-sh/argsh")" || { echo "argsh: failed to get latest release from GitHub" >&2;return 1;};local _asset="argsh-linux-${_arch}.so";echo "argsh: downloading ${_asset} (${_tag})..." >&2;curl -fsSL -o "${_dest}" "https://github.com/arg-sh/argsh/releases/download/${_tag}/${_asset}" || { echo "argsh: download failed" >&2;echo "  Asset ${_asset} may not exist for ${_tag}" >&2;rm -f "${_dest}";return 1;};echo "argsh: installed to ${_dest}" >&2;argsh::builtin::try "${_dest}" || { echo "argsh: downloaded file failed to load as builtin" >&2;rm -f "${_dest}";return 1;};return 0;};argsh::builtin() { case "${1:-}" in
install)shift; argsh::builtin::_install "${@}";;
update)shift; argsh::builtin::_install --force "${@}";;
status|"")local _loc _arch;_loc="$(argsh::builtin::location 2>/dev/null)" || _loc="not installed";_arch="$(argsh::builtin::arch 2>/dev/null)" || _arch="unsupported";echo "argsh builtin: ${_loc}";echo "  platform: $(uname -s | tr '[:upper:]' '[:lower:]')/${_arch}";echo "  loaded:   $(( ${ARGSH_BUILTIN:-0} )) (ARGSH_BUILTIN=${ARGSH_BUILTIN:-0})";echo "";echo "Usage: argsh builtin [install|update|status] [--force] [--path DIR]";echo "       Set ARGSH_BUILTIN_PATH env var to control builtin search path.";;
*)echo "argsh: unknown builtin subcommand: ${1}" >&2;echo "Usage: argsh builtin [install|update|status] [--force] [--path DIR]" >&2;return 1;;
esac;};argsh::builtins() { argsh::builtin "${@}"; };argsh::discover_dirs() { local -a _raw_dirs=();_search_dirs=();local _d _existing _skip _rd _re;if [[ -n "${PATH_TEST:-}" ]];then IFS=';' read -ra _raw_dirs <<< "${PATH_TEST}";fi;_raw_dirs+=( "${BASH_SOURCE[0]%/*}" "${PATH_BASE:-.}" "${PATH_BASE:-.}/test" "${PATH_BASE:-.}/tests" "${PATH_BASE:-.}/libraries" );for _d in "${_raw_dirs[@]}";do [[ -d "${_d}" ]] || continue;_skip=0;_rd="$(realpath "${_d}" 2>/dev/null || echo "${_d}")";for _existing in "${_search_dirs[@]}";do _re="$(realpath "${_existing}" 2>/dev/null || echo "${_existing}")";[[ "${_rd}" != "${_re}" ]] || { _skip=1; break; };done;(( _skip )) || _search_dirs+=("${_d}");done;};argsh::discover_files() { local -a _search_dirs=();argsh::discover_dirs;local _d _f _pattern;for _d in "${_search_dirs[@]}";do [[ -d "${_d}" ]] || continue;for _pattern in "${@}";do for _f in "${_d}"/${_pattern};do [[ -f "${_f}" ]] || continue;_found_files+=("${_f}");done;done;done;};argsh::status() { echo "argsh ${ARGSH_VERSION:-unknown} (${ARGSH_COMMIT_SHA:-unknown})";echo "  script: $(realpath "${BASH_SOURCE[0]}" 2>/dev/null || echo "${BASH_SOURCE[0]}")";echo "";local _loc _arch _so_status;_loc="$(argsh::builtin::location 2>/dev/null)" || _loc="not installed";_arch="$(argsh::builtin::arch 2>/dev/null)" || _arch="unsupported";if (( ${ARGSH_BUILTIN:-0} ));then _so_status="loaded";else _so_status="not loaded";fi;echo "Builtin (.so):";echo "  status:       ${_so_status}";echo "  path:         ${_loc}";echo "  architecture: $(uname -s | tr '[:upper:]' '[:lower:]')/${_arch}";echo "";echo "Shell:";echo "  bash: ${BASH_VERSION:-unknown}";echo "";echo "Features:";if (( ${ARGSH_BUILTIN:-0} ));then echo "  mcp:        available (builtin)";echo "  completion: available (builtin)";echo "  docgen:     available (builtin)";else echo "  mcp:        requires builtin";echo "  completion: requires builtin";echo "  docgen:     requires builtin";fi;echo "";local -a _found_files=();argsh::discover_files "*.bats";if (( ${#_found_files[@]} > 0 ));then echo "Tests: ${#_found_files[@]} .bats file(s)";local _f;for _f in "${_found_files[@]}";do echo "  ${_f}";done;else echo "Tests: none found";fi;local -a _cov_files=();local _d _cov_file;for _d in "${PATH_BASE:-.}" "${PATH_BASE:-.}"/*/;do _d="${_d%/}";[[ -f "${_d}/coverage.json" ]] && _cov_files+=("${_d}/coverage.json");done;if (( ${#_cov_files[@]} > 0 ));then echo "Coverage:";for _cov_file in "${_cov_files[@]}";do local _pct="?" _date="?";_pct="$(grep -o '"percent_covered"[^"]*"[^"]*"' "${_cov_file}" | tail -1)" && [[ "${_pct}" =~ \"([0-9.]+)\"$ ]] && _pct="${BASH_REMATCH[1]}";_date="$(grep -o '"date"[^"]*"[^"]*"' "${_cov_file}")" && [[ "${_date}" =~ \"([^\"]+)\"$ ]] && _date="${BASH_REMATCH[1]}";echo "  ${_cov_file##"${PATH_BASE:-.}"/}: ${_pct}% (${_date})";done;else echo "Coverage: no coverage.json found";fi;};argsh::_docker_forward() { binary::exists docker || { echo "argsh: this command requires either the tool installed locally or Docker" >&2;return 1;};local a78="-i";! tty -s || a78="-it";local -r a36="${ARGSH_DOCKER_IMAGE:-ghcr.io/arg-sh/argsh:a20}";docker run --rm ${a78} $(docker::user) -e "BATS_LOAD" -e "ARGSH_SOURCE" -e "PATH_TEST" -e "GIT_COMMIT_SHA=$(git rev-parse HEAD 2>/dev/null || :)" -e "GIT_VERSION=$(git describe --tags --dirty 2>/dev/null || :)" "${a36}" "${@}";};argsh::minify() { if ! binary::exists minifier;then argsh::_docker_forward minify "${@}";return;fi;local template out="/dev/stdout";local -a files ignore_variable args=( 'files'              "Files to minify, can be a glob pattern" 'template|t:~file'   "Path to a template file to use for the minified file" 'out|o'              "Path to the output file" 'ignore-variable|i'  "Ignores specific variable names from obfuscation" );:args "Minify Bash files" "${@}";! is::uninitialized files || { :args::error_usage "No files to minify";return 1;};local _content _tout;_content="$(mktemp)";_tout="$(mktemp)";trap "rm -f ${_content} ${_tout}" EXIT;local _f _file;local -a _glob;for _f in "${files[@]}";do if [[ -d "${_f}" ]];then _glob=("${_f}"/*.{sh,bash});else _glob=(${_f});fi;for _file in "${_glob[@]}";do [[ -e "${_file}" ]] || continue;{ cat "${_file}";echo;} >>"${_content}";done;done;local -a _iVars=();if ! is::uninitialized ignore_variable && (( ${#ignore_variable[@]} ));then _iVars=(-I "$(array::join "," "${ignore_variable[@]}")");fi;minifier -i "${_content}" -o "${_tout}" -O "${_iVars[@]}";local -r data="$(cat "${_tout}")";if [[ -z "${template:-}" ]];then echo -n "${data}" >"${out}";return 0;fi;local commit_sha="${GIT_COMMIT_SHA:-}";local version="${GIT_VERSION:-}";export data commit_sha version;envsubst '$data,$commit_sha,$version' <"${template}" >"${out}";};argsh::lint() { if ! binary::exists shellcheck;then argsh::_docker_forward lint "${@}";return;fi;local -a files args=( 'files'  "Files to lint, can be a glob pattern" );:args "Lint Bash files" "${@}";if is::uninitialized files;then local -a _found_files=();argsh::discover_files "*.sh" "*.bash" "*.bats";local -a _search_dirs=();argsh::discover_dirs;local _d _f;for _d in "${_search_dirs[@]}";do [[ -d "${_d}" ]] || continue;for _f in "${_d}"/*;do [[ -f "${_f}" ]] || continue;local _basename="${_f##*/}";[[ "${_basename}" != *.* ]] || continue;local _shebang;_shebang="$(head -1 "${_f}" 2>/dev/null || :)";if [[ "${_shebang}" == "#!"*bash* || "${_shebang}" == "#!"*"/sh"* || "${_shebang}" == "#!"*argsh* ]];then _found_files+=("${_f}");fi;done;done;if (( ${#_found_files[@]} == 0 ));then echo "No files to lint (set PATH_TEST or pass files as arguments)" >&2;return 1;fi;files=("${_found_files[@]}");fi;local _file _f;local -a _glob;for _f in "${files[@]}";do if [[ -d "${_f}" ]];then _glob=("${_f}"/*.{sh,bash,bats});else _glob=(${_f});fi;for _file in "${_glob[@]}";do [[ -e "${_file}" ]] || continue;echo "Linting ${_file}" >&2;shellcheck "${_file}";done;done;};argsh::test() { if ! binary::exists bats;then argsh::_docker_forward test "${@}";return;fi;local -a path args=( 'path'  "Path to the bats test files" );:args "Run tests" "${@}";if is::uninitialized path;then local -a _found_files=();argsh::discover_files "*.bats";if (( ${#_found_files[@]} == 0 ));then echo "No test files found (set PATH_TEST or pass files as arguments)" >&2;return 1;fi;path=("${_found_files[@]}");fi;[[ -z "${BATS_LOAD:-}" ]] || echo "Running tests for ${BATS_LOAD}" >&2;bats "${path[@]}";};argsh::coverage() { if ! binary::exists kcov;then argsh::_docker_forward coverage "${@}";return;fi;local out="./coverage" min=75;local -a tests=(".") args=( 'tests'     "Path to the bats test files" 'out|o'     "Path to the output directory" 'min|:~int' "Minimum coverage required" );:args "Generate coverage report for your Bash scripts" "${@}";echo "Generating coverage report for: ${tests[*]}" >&2;kcov --clean --bash-dont-parse-binary-dir --include-pattern=.sh --exclude-pattern=tests --include-path=. "${out}" bats "${tests[@]}" >/dev/null 2>&1 || { echo "Failed to generate coverage report";echo "Run tests with 'argsh test' to see what went wrong";return 1;} >&2;cp "${out}"/bats.*/coverage.json "${out}/coverage.json";local coverage;coverage="$(jq -r '.percent_covered | tonumber | floor' "${out}/coverage.json")";echo "Coverage is ${coverage}% of required ${min}%";(( coverage > min )) || return 1;};argsh::docs() { if ! binary::exists shdoc;then argsh::_docker_forward docs "${@}";return;fi;local in out prefix="";local -a args=( 'in'      "Path to the source files to generate documentation from, can be a glob pattern" 'out'     "Path to the output directory" 'prefix'  "Prefix for each md file" );:args "Generate documentation" "${@}";[[ -d "${out}" ]] || { :args::error_usage "out is not a directory";return 1;};local -a a3=(-o "${out}");[[ -z "${prefix}" ]] || a3+=(-p "${prefix}");shdoc "${a3[@]}" ${in};};argsh::main() { local -a usage=( '-'                          "Tools" 'minify:-argsh::minify'      "Minify Bash files" 'lint:-argsh::lint'          "Lint Bash files" 'test:-argsh::test'          "Run tests" 'coverage:-argsh::coverage'  "Generate coverage report for your Bash scripts" 'docs:-argsh::docs'          "Generate documentation" '-'                          "Runtime" 'builtin:-argsh::builtin'    "Manage native builtins (.so)" 'status:-argsh::_status_cmd' "Show argsh runtime status" );:usage "Enhance your Bash scripting by promoting structure and maintainability,"$'\n'"making it easier to write, understand,"$'\n'"and maintain even complex scripts." "${@}";"${usage[@]}";};argsh::_status_cmd() { declare -gi ARGSH_BUILTIN=0;if (( ${_argsh_no_builtin:-0} == 0 )) && declare -p __ARGSH_BUILTINS &>/dev/null;then argsh::builtin::try && ARGSH_BUILTIN=1;fi;argsh::status "${@}";};argsh::builtin::_install() { local _force=0 _dest_dir="";while [[ "${1:-}" == --* ]];do case "${1}" in
--force)_force=1; shift;;
--path)shift;if [[ -z "${1:-}" || "${1}" == --* ]];then echo "argsh: --path requires a directory argument" >&2;return 1;fi;_dest_dir="${1}"; shift;;
*)echo "argsh: unknown option: ${1}" >&2; return 1;;
esac;done;if [[ -n "${_dest_dir}" ]];then [[ -d "${_dest_dir}" && -w "${_dest_dir}" ]] || { echo "argsh: directory not writable: ${_dest_dir}" >&2;return 1;};PATH_BIN="${_dest_dir}" argsh::builtin::download "${_force}";else argsh::builtin::download "${_force}";fi;};argsh::shebang() { local -a _argsh_imports=();local _argsh_no_builtin=0;while [[ "${1:-}" == -* ]];do case "${1}" in
--help|-h)(argsh::main --help) || true;return 0;;
--import|-i)shift;[[ -n "${1:-}" ]] || { echo "argsh: --import requires an argument" >&2; return 1; };_argsh_imports+=("${1}");shift;;
--no-builtin)_argsh_no_builtin=1;shift;;
--version)echo "argsh ${ARGSH_VERSION:-unknown} (${ARGSH_COMMIT_SHA:-unknown})";return 0;;
--)shift; break;;
*)break;;
esac;done;if [[ -z "${1:-}" ]];then (argsh::main --help) || true;return 0;fi;local -r a56="${1}";: "${ARGSH_SOURCE="${a56}"}";export ARGSH_SOURCE;if [[ "${BASH_SOURCE[-1]}" == "${a56}" || ! -f "${a56}" ]];then if [[ "${a56}" == "builtins" ]];then shift;argsh::builtin "${@}";return;fi;argsh::main "${@}";return;fi;bash::version 4 3 0 || { echo "This script requires bash 4.3.0 or later";return 1;} >&2;declare -gi ARGSH_BUILTIN=0;if (( ! _argsh_no_builtin ));then [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: searching for argsh.so..." >&2;if argsh::builtin::try;then ARGSH_BUILTIN=1;[[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: loaded builtins from $(argsh::builtin::location 2>/dev/null || echo 'unknown')" >&2;else [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: builtins not found locally" >&2;if [[ "${ARGSH_NO_AUTO_DOWNLOAD:-}" != "1" ]];then [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: attempting auto-download of builtins" >&2;argsh::builtin::download 0 && argsh::builtin::try && ARGSH_BUILTIN=1;else [[ "${ARGSH_DEBUG:-}" == "1" ]] && echo "argsh:debug: auto-download disabled (ARGSH_NO_AUTO_DOWNLOAD=1)" >&2;fi;fi;fi;local _lib;for _lib in "${_argsh_imports[@]}";do import "${_lib}";done;shift;. "${a56}";};string::drop-index() { local a25="${1}";local a37="${2}";local a21="${3:-1}";echo "${a25:0:a37}${a25:a37+a21}";};string::random() { local a21="${1:-42}";local a32="${2:-"a-zA-Z0-9"}";local a76;until [[ "${a76:-}" =~ ^[[:alpha:]] ]];do a76=$(tr -dc "${a32}" < /dev/urandom | fold -w "${a21}" | head -n 1 || :);done;echo "${a76}";};string::indent() { local a25="${1:-'-'}";local a19="${2:-0}";local a60 a39;[[ ${a25} != '-' ]] || a25="$(cat)";mapfile -t a39 < <(echo "${a25}");for a60 in "${a39[@]}";do a60="$(string::trim-left "${a60}")";(( a19 == 0 )) || printf "%${a19}s" " ";echo "${a60}";done;};string::trim-left() { local a25="${1}";local a32="${2:-" "$'\n'$'\t'}";[[ -n ${a25:-} ]] || return 0;[[ ${a25} != '-' ]] || a25="$(cat)";while [[ -n "${a25}" ]];do [[ ${a32} == *"${a25:0:1}"* ]] || break;a25="${a25:1}";done;echo "${a25}";};string::trim-right() { local a25="${1:-'-'}";local a32="${2:-" "$'\n'$'\t'}";[[ ${a25} != '-' ]] || a25="$(cat)";while [[ -n "${a25}" ]];do [[ ${a32} == *"${a25: -1}"* ]] || break;a25="${a25:0: -1}";done;echo "${a25}";};string::trim() { local a25="${1:-'-'}";local a32="${2:-" "$'\n'$'\t'}";[[ ${a25} != '-' ]] || a25="$(cat)";echo "${a25}" | string::trim-left - "${a32}" | string::trim-right - "${a32}";};to::string() { local a47="${1}";echo "${a47}";};to::boolean() { local a47="${1}";case "${a47}" in
""|"false"|"0")a47="0";;
*)a47="1";;
esac;echo "${a47}";};to::int() { local a47="${1}";[[ ${a47} =~ ^-?[0-9]+$ ]] || return 1;echo "${a47}";};to::float() { local a47="${1}";[[ ${a47} =~ ^-?[0-9]+(\.[0-9]+)?$ ]] || return 1;echo "${a47}";};to::stdin() { local a47="${1}";[[ ${a47} != "-" ]] || a47="$(cat)";echo "${a47}";};to::file() { local a47="${1}";[[ -f "${a47}" ]] || return 1;echo "${a47}";};[[ "${BASH_SOURCE[0]}" != "${0}" ]] || argsh::shebang "${@}"