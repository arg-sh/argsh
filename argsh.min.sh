#!/usr/bin/env bash
# shellcheck disable=SC2178 disable=SC2120 disable=SC1090 disable=SC2046 disable=SC2155 disable=SC2154 disable=SC2329
set -euo pipefail; ARGSH_COMMIT_SHA="e03f7778b2d6ca1c6e10e2bf5d3527880e6cb590"; ARGSH_VERSION="v0.2.1-61-ge03f777-dirty"
: "${ARGSH_VERSION:=unknown}";: "${ARGSH_COMMIT_SHA:=unknown}";: "${ARGSH_FIELD_WIDTH:=24}";COMMANDNAME=("$(s="${ARGSH_SOURCE:-"${0}"}"; echo "${s##*/}")");declare -ga __ARGSH_BUILTINS=(:usage :args is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string import import::clear);declare -g __ARGSH_LIB_DIR="${BASH_SOURCE[0]%/*}";__argsh_try_builtin() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] || continue;enable -f "${_so}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] || continue;enable -f "${_so}/${_n}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || continue;return 0;done;done;return 1;};declare -gi ARGSH_BUILTIN="${ARGSH_BUILTIN:-0}";if __argsh_try_builtin;then ARGSH_BUILTIN=1;unset -f import 2>/dev/null || true;[[ "$(type -t import 2>/dev/null)" == "builtin" ]] || import() { declare -A _i; (( ${_i[${1}]:-} )) || { _i[${1}]=1; . "${BASH_SOURCE[0]%/*}/${1}.sh"; } };fi;if (( ARGSH_BUILTIN ));then unset -f is::array is::uninitialized is::set is::tty args::field_name to::int to::float to::boolean to::file to::string 2>/dev/null || true;fi;if ! (( ARGSH_BUILTIN ));then :usage() { local a35="${1}"; shift;declare -p usage &>/dev/null || local -a usage=();declare -p args &>/dev/null || local -a args=();[[ $(( ${#usage[@]} % 2 )) -eq 0 ]] || :args::_error "usage must be an associative array";if [[ -z ${1:-} || ${1} == "-h" || ${1} == "--help" ]];then :usage::text "${a35}";exit 0;fi;if ! (( ${#COMMANDNAME[@]} )) && [[ ${1:-} == "--argsh" ]];then echo "https://arg.sh ${ARGSH_COMMIT_SHA:-} ${ARGSH_VERSION:-}";exit 0;fi;local -A a30=();local -a a54=("${@}");local a55 a24="";while (( ${#a54[@]} ));do if [[ ${a54[0]:0:1} != "-" ]];then [[ -z "${a55:-}" ]] || break;a55="${a54[0]}";a54=("${a54[@]:1}");continue;fi;:args::parse_flag || break;a30["${a24}"]=1;done;:args::check_required_flags;local a44;for (( a68=0; a68 < ${#usage[@]}; a68+=2 ));do IFS='|' read -ra _aliases <<< "${usage[a68]/:*}";for a20 in "${_aliases[@]}";do a20="${a20#\#}";[[ "${a55}" == "${a20}" ]] || continue;a24="${usage[a68]#\#}";a44="${usage[a68]/*:-}";a44="${a44#\#}";[[ "${a44}" == "${usage[a68]}" ]] || break 2;a44="${a44/|*}";break 2;done;done;[[ -n "${a44:-}" ]] || :args::error_usage "Invalid command: ${a55}";local a6=0;[[ "${a24}" != *":-"* ]] || a6=1;if (( a6 ));then declare -F -- "${a44}" >/dev/null 2>&1 || :args::error_usage "Invalid command: ${a55}";else local a10="${FUNCNAME[1]:-}";if [[ -n "${a10}" ]] && declare -F -- "${a10}::${a44}" >/dev/null 2>&1;then a44="${a10}::${a44}";elif declare -F -- "argsh::${a44}" >/dev/null 2>&1;then a44="argsh::${a44}";elif declare -F -- "${a44}" >/dev/null 2>&1;then :;else :args::error_usage "Invalid command: ${a55}";fi;fi;COMMANDNAME+=("${a24/[|:]*}");usage=("${a44}" "${a54[@]}");};:usage::text() { local a35="${1:-}";string::indent "${a35}";echo;echo "Usage: ${COMMANDNAME[*]} <command> [args]";[[ ${usage[0]:-} == '-' ]] || echo -e "\nAvailable Commands:";for (( a68=0; a68 < ${#usage[@]}; a68+=2 ));do [[ "${usage[a68]:0:1}" != "#" ]] || continue;[[ "${usage[a68]}" != "-" ]] || { echo;echo "${usage[a68+1]}";continue;};printf "  %-${ARGSH_FIELD_WIDTH}s %s\n" "${usage[a68]/[:|]*}" "${usage[a68+1]}";done;:args::text_flags;echo;echo "Use \"${COMMANDNAME[*]} <command> --help\" for more information about a command.";};:args() { local a35="${1}"; shift;declare -p args &>/dev/null || local -a args=();[[ $(( ${#args[@]} % 2 )) -eq 0 ]] || :args::_error "args must be an associative array";if [[ ${1:-} == "-h" || ${1:-} == "--help" ]];then :args::text;exit 0;fi;local a25=0 a24="" a68 a0=1;local -A a30=();local -a a54=("${@}");while (( ${#a54[@]} ));do if [[ ${a54[0]:0:1} != "-" ]];then local a48 a36;a68="$(:args::field_positional "${a0}")" || :args::error_usage "too many arguments: ${a54[0]}";a24="${args[a68]}";a48="$(args::field_name "${a24}")";a36="$(:args::field_value "${a54[0]}")" || exit "${?}";local -n a60="${a48}";if is::array "${a48}";then (( a25 )) || { a60=();a25=1;};a60+=("${a36}");else a60="${a36}";fi;a54=("${a54[@]:1}");(( ++a0 ));continue;fi;:args::parse_flag || :args::error_usage "unknown a43: ${a54[0]}";a30["${a24}"]=1;done;if a68="$(:args::field_positional "${a0}")";then a24="$(args::field_name "${args[a68]}")";if is::uninitialized "${a24}" && ! is::array "${a24}";then :args::error_usage "missing required argument: ${a24}";fi;fi;:args::check_required_flags;[[ ${#a54[@]} -eq 0 ]] || :args::error_usage "too many arguments: ${a54[*]}";};:args::text() { declare -p args &>/dev/null || return 0;local -a a2=() a16=();:args::positional;string::indent "${a35}";echo;echo "Usage:";echo "  ${COMMANDNAME[*]} ${a16[*]}";(( ${#a2[@]} == 0 )) || { echo;echo "Arguments:";for a68 in "${a2[@]}";do [[ ${args[a68]} != "-" ]] || continue;a41="$( printf "   %-${ARGSH_FIELD_WIDTH}s%s" " " "${args[a68+1]}" | fmt::tty;)";printf "   %-${ARGSH_FIELD_WIDTH}s%s\n" "$(:args::fieldf "${args[a68]}")" "$(string::trim-left "${a41}")";done;};:args::text_flags;echo;};:args::text_flags() { local -a args=("${args[@]}");local -a a26=();array::contains 'help|h:+' "${args[@]}" || args+=('help|h:+' "Show this help message");:args::flags;(( ${#a26[@]} )) || return 0;[[ "${args[${a26[0]}]}" == "-" ]] || echo -e "\nOptions:";for a68 in "${a26[@]}";do [[ "${args[a68]:0:1}" != "#" ]] || continue;[[ "${args[a68]}" != "-" ]] || { echo;echo "${args[a68+1]}";continue;};:args::fieldf "${args[a68]}";{ echo -n "           ";echo -e "${args[a68+1]}\n";} | fmt::tty;done;};:args::flags() { declare -p args &>/dev/null || local -a args;declare -p a26 &>/dev/null || local -a a26;for (( a68=0; a68 < "${#args[@]}"; a68+=2 ));do if [[ ${args[a68]} == *"|"* || ${args[a68]} == '-' ]];then a26+=("${a68}");fi;done;};:args::positional() { declare -p args &>/dev/null || local -a args;declare -p a2 &>/dev/null || local -a a2;declare -p a16 &>/dev/null || local -a a16;local a60;for (( a68=0; a68 < "${#args[@]}"; a68+=2 ));do [[ ${args[a68]} != *"|"* && ${args[a68]} != '-' ]] || continue;a60="$(args::field_name "${args[a68]}")";a2+=("${a68}");if is::array "${a60}";then a16+=("...${a60}");continue;fi;if ! is::uninitialized "${a60}";then a16+=("[${a60}]");continue;fi;a16+=("<${a60}>");done;};:args::parse_flag() { declare -p a54 a24 &>/dev/null || return 1;local a43="${a54[0]/=*}";if [[ ${a43:0:2} == "--" ]];then a24="$(:args::field_lookup "${a43:2}")" || return "${?}";elif [[ ${a43:0:1} == "-" ]];then a43="${a43:0:2}";a24="$(:args::field_lookup "${a43:1}")" || return "${?}";fi;:args::field_set_flag "${a24}";};:args::check_required_flags() { declare -p a30 args &>/dev/null || return 1;local a24;local -a a22;for (( a68=0; a68 < ${#args[@]}; a68+=2 ));do a24="${args[a68]}";:args::field_attrs "${a24}";if (( a22[2] )) && ! (( a22[4] ));then local -n a60="${a22[0]}";a60=0;fi;if (( a22[6] )) && [[ -z ${a30[${args[a68]}]:-} ]];then :args::error_usage "missing required a43: ${args[a68]/|*}";fi;done;};:args::field_set_flag() { local a24="${1}";declare -p a54 a43 &>/dev/null || return 1;local -a a22;:args::field_attrs "${a24}";local -n a60="${a22[0]}";local a5 a3;if (( a22[2] ));then a5=1;if [[ ${a43:0:2} == "--" ]];then a54=("${a54[@]:1}");else a54[0]="-${a54[0]:2}";[[ ${a54[0]} != "-" ]] || a54=("${a54[@]:1}");fi;fi;[[ -n ${a5:-} ]] || { a3="${a54[0]/${a43}}";if [[ ${a3} == "" ]];then (( ${#a54[@]} > 1 )) || :args::error "missing value for a43: ${a22[0]}";a5="${a54[1]}";a54=("${a54[@]:1}");else [[ "${a3:0:1}" != "=" ]] || a3="${a3:1}";a5="${a3}";fi;a5="$(:args::field_value "${a5}")" || exit "${?}";a54=("${a54[@]:1}");};if (( a22[5] ));then a60+=("${a5}");else a60="${a5}";fi;};:args::field_value() { local a36="${1}";declare -p a24 &>/dev/null || return 1;declare -p a22 &>/dev/null || { local -a a22;:args::field_attrs "${a24}";};declare -f "to::${a22[3]}" &>/dev/null || :args::_error "unknown type: ${a22[3]}";"to::${a22[3]}" "${a36}" "${a22[0]}" || :args::error_usage "invalid type (${a22[3]}): ${a36}";};:args::field_lookup() { local a24="${1}";declare -p args &>/dev/null || return 1;for (( a68=0; a68 < ${#args[@]}; a68+=2 ));do if [[ ${args[a68]} =~ (^${a24}\||\|${a24}:|\|${a24}$) ]];then echo "${args[a68]}";return 0;fi;done;return 1;};:args::field_positional() { local a7="${1:-1}";declare -p args &>/dev/null || return 1;for (( a68=0; a68 < ${#args[@]}; a68+=2 ));do if [[ ${args[a68]} != *"|"* && ${args[a68]} != '-' ]];then if is::array "$(args::field_name "${args[a68]}")" || (( --a7 == 0 ));then echo "${a68}";return 0;fi;fi;done;return 1;};args::field_name() { local a24="${1}";local a21="${2:-1}";a24="${a24/[|:]*}";a24="${a24#\#}";if (( a21 ));then a24="${a24//-/_}";fi;echo "${a24}";};:args::field_attrs() { local a24="${1}";declare -p a22 &>/dev/null || local -a a22;a22=( "" "" 0 "" 0 0 0 0 "" );local a51="+~!";local a47="${a24#*[:]}";[ "${a47}" != "${a24}" ] || a47="";a22[0]="$(args::field_name "${a24}")";a22[8]="$(args::field_name "${a24}" 0)";[[ ${a22[0]:0:1} != "#" ]] || { a22[7]=1;};local -n a60="${a22[0]}";local -a a26;IFS='|' read -ra a26 <<< "${a24/[:]*}";[[ ${#a26[@]} -eq 1 ]] || { a22[1]="${a26[1]}";};if is::array "${a22[0]}";then a22[5]=1;! is::uninitialized "${a22[0]}" || a60=();! (( ${#a60[@]} )) || a22[4]=1;elif ! is::uninitialized "${a22[0]}";then a22[4]=1;fi;while (( ${#a47} > 0 ));do if [[ ${a47:0:1} == "+" ]];then [[ -z ${a22[3]} ]] || :args::_error "cannot have multiple types: ${a22[3]} and boolean";a22[2]=1;a47="${a47:1}";continue;fi;if [[ ${a47:0:1} == "~" ]];then ! (( a22[2] )) || :args::_error "already flagged as boolean";a47="${a47:1}";a22[3]="${a47/[$a51]*}";a47="${a47:${#a22[3]}}";continue;fi;if [[ ${a47:0:1} == "!" ]];then ! (( a22[6] )) || :args::_error "field already flagged as required";a22[6]=1;a47="${a47:1}";continue;fi;echo ":args error: unknown modifier: ${a47:0:1}" >&2;exit 2;done;if [[ -z ${a22[3]} && ${a22[2]} -eq 0 ]];then a22[3]="string";fi;};:args::fieldf() { local a24="${1}";declare -p a22 &>/dev/null || { local -a a22;:args::field_attrs "${a24}";};[[ ${a24} == *"|"* ]] || { echo "${a22[8]} ${a22[3]}";return 0;};local -n a60="${a22[0]}";a11="   ";! (( a22[6] )) || a11=" ! ";if [[ -n ${a22[1]} ]];then a11+="-${a22[1]}, --${a22[8]}";else a11+="    --${a22[8]}";fi;a11+=" ";! (( a22[5] )) || a11+="...";a11+="${a22[3]}";if (( a22[4] )) && ! (( a22[2] ));then a11+=" (default: ${a60[*]})";fi;echo "${a11}";};fi;args::run() { local a53="${1}"; shift;for (( a68=0; a68<${#}; a68++ ));do local a61="${1}"; shift;local a44="${1}"; shift;if ! (( a53 )) || (( a61 ));then "${a44}";fi;done;};array::contains() { local -r a15="${1}"; shift;for a8 in "${@}";do [[ "${a8}" != "${a15}" ]] || return 0;done;return 1;};array::join() { local -r a4="${1}"; shift;local a17;printf -v a17 "${a4}%s" "${@}";echo "${a17:${#a4}}";};array::nth() { local -n a59="${1}";local -r a58="${2}";shift 2;for (( a68=1; a68<=${#}; a68++ ));do (( a68 % a58 )) || a59+=("${!a68}");done;};bash::version() { local a29="${1:-4}";local a31="${2:-3}";local a32="${3:-0}";if [[ "${BASH_VERSINFO[0]}" -lt "${a29}" ]];then return 1;elif [[ "${BASH_VERSINFO[0]}" -gt "${a29}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[1]}" -lt "${a31}" ]];then return 1;elif [[ "${BASH_VERSINFO[1]}" -gt "${a31}" ]];then return 0;fi;if [[ "${BASH_VERSINFO[2]}" -lt "${a32}" ]];then return 1;fi;return 0;};binary::exists() { local a9="${1}";command -v "${a9}" &> /dev/null || { echo "${a9} is required to run this script" >&2;return 1;};};binary::github() { local a49="${1}";local a50="${2}";local a42="${3}";local a64="${4:-}";curl -Lso /dev/stdout "https://github.com/${a50}/releases/download/${a42}" | { if [[ -n "${a64}" ]];then tar -xz -C "$(dirname "${a49}")" "${a64}";else tee "${a49}" &> /dev/null;fi;chmod +x "${a49}";};};binary::arch() { local a34="${1:-0}";local -r a37="$(uname -m)";case "${a37}" in
x86_64|amd64)if (( a34 )); then echo "64-bit"; else echo "amd64"; fi;;
armv7l)echo "arm";;
aarch64)echo "arm64";;
*)echo "${a37}";;
esac;};binary::jq() { binary::exists "jq" 2>/dev/null || { local -r a13="$(github::latest "stedolan/jq")" a19="$(uname -s)";binary::github "${PATH_BIN?}/jq" "stedolan/jq" "${a13}/jq-${a19,,}-$(binary::arch)";};};binary::vale() { binary::exists "vale" 2>/dev/null || { local -r a13="$(github::latest "errata-ai/vale")" a19="$(uname -s)";binary::github "${PATH_BIN?}/vale" "errata-ai/vale" "${a13}/vale_${a13:1}_$(uname -s)_$(binary::arch 1).tar.gz" "vale";};};docker::user() { local a66="${1:-"$(id -u)"}";local a57="${2:-"$(id -g)"}";local a52="${3:-"$(whoami)"}";local a45="${4:-"/workspace"}";local a33="${5:-"/bin/sh"}";local a40;a40="$(pwd)";a40="${a40#"${PATH_BASE:-}"}";if [[ "${a40}" == "$(pwd)" ]];then a40="${a45}";else a40="${a45}${a40}";fi;local _passwd _group;_passwd="$(mktemp /tmp/docker_passwd.XXXXXX)";_group="$(mktemp /tmp/docker_group.XXXXXX)";echo "${a52}:x:${a66}:${a57}::${a45}:${a33}" > "${_passwd}";echo "${a52}:x:${a57}:" > "${_group}";echo "-v ${_passwd}:/etc/passwd -v ${_group}:/etc/group";echo "-u ${a66}:${a57}";echo "-v ${PATH_BASE:-"$(pwd)"}:${a45}";echo "-w ${a40}";};error::stacktrace() { local -r a38="${1:-${?}}";if (( a38 ));then echo -e "\n\033[38;5;196m■■ Stacktrace(${a38}): \e[1m${BASH_COMMAND}\e[22m";for (( a68 = 1; a68 <= ${#FUNCNAME[@]} - 2; a68++ ));do echo -e "${a68}. ${BASH_SOURCE[a68]}:${BASH_LINENO[a68-1]} ➜ ${FUNCNAME[a68]}()";done;echo -e "\033[0m";return "${a38}";fi;};:args::_error() { declare -p a24 &>/dev/null || local a24="???";echo ":args error [${a24}] ➜ ${1}" >&2;exit 2;};:args::error() { declare -p a24 &>/dev/null || local a24="???";echo -e "[ ${a24/[:|]*} ] invalid argument\n➜ ${1}\n" >&2;exit 2;};:args::error_usage() { declare -p a24 &>/dev/null || local a24="???";echo -e "[ ${a24/[:|]*} ] invalid usage\n➜ ${1}\n" >&2;echo -e "Use \"${0##*/} -h\" for more information" >&2;exit 2;};fmt::tty() { local a63="${1:-"$(cat)"}";if ! command -v fmt &>/dev/null || [[ ! -t 1 ]];then echo "${a63}";return 0;fi;local a39;a39="$(tput cols)";echo "${a63}" | fmt -w "${a39}";};github::latest() { local a50="${1}";curl -fsSLI -o /dev/null -w "%{url_effective}" "https://github.com/${a50}/releases/latest" | rev | cut -d'/' -f1 | rev;};declare -gA a1=();: "${__ARGSH_LIB_DIR:=${BASH_SOURCE[0]%/*}}";import() { local a62="${1}";(( ${a1["${a62}"]:-} )) || { a1["${a62}"]=1;if [[ ${a62:0:1} == "@" ]];then a62="${PATH_BASE:?"PATH_BASE missing"}/${a62:1}";elif [[ ${a62:0:1} == "~" ]];then local _s="${ARGSH_SOURCE:-${BASH_SOURCE[-1]}}";a62="${_s%/*}/${a62:1}";else local _s="${ARGSH_SOURCE:-${BASH_SOURCE[0]}}";a62="${_s%/*}/${a62}";fi;import::source "${a62}" || exit 1;};};import::source() { local a62="${1}";for a56 in "" ".sh" ".bash";do if [[ -f "${a62}${a56}" ]];then . "${a62}${a56}";return;fi;done;echo "Library not found ${a62}" >&2;return 1;};import::clear() { a1=();};is::tty() { [[ -t 1 ]];};is::array() { local _decl;_decl="$(declare -p "${1}" 2>/dev/null)" || return 1;[[ "${_decl}" == "declare -a"* ]];};is::uninitialized() { local a67="${1}";if is::array "${a67}";then [[ $(declare -p "${a67}") =~ ^declare\ -a\ ${a67}(=\(\))?$ ]];else [[ ! ${!a67+x} ]];fi;};is::set() { ! is::uninitialized "${1}";};argsh::builtin::try() { if [[ -n "${1:-}" ]];then [[ -f "${1}" ]] || return 1;enable -f "${1}" "${__ARGSH_BUILTINS[@]}" 2>/dev/null || return 1;return 0;fi;__argsh_try_builtin;};argsh::builtin::location() { local _so _d;local -r _n="argsh.so";for _so in "${ARGSH_BUILTIN_PATH:-}" "${PATH_LIB:+${PATH_LIB}/${_n}}" "${PATH_BIN:+${PATH_BIN}/${_n}}" ;do [[ -n "${_so}" && -f "${_so}" ]] && { echo "${_so}"; return 0; };done;for _d in "${LD_LIBRARY_PATH:-}" "${BASH_LOADABLES_PATH:-}";do [[ -n "${_d}" ]] || continue;local IFS=:;for _so in ${_d};do [[ -n "${_so}" && -f "${_so}/${_n}" ]] && { echo "${_so}/${_n}"; return 0; };done;done;[[ -f "${HOME}/.local/lib/bash/${_n}" ]] && { echo "${HOME}/.local/lib/bash/${_n}"; return 0; };echo "not installed";return 1;};argsh::builtin::install_dir() { local _d;if [[ -n "${PATH_BIN:-}" && -d "${PATH_BIN}" && -w "${PATH_BIN}" ]];then echo "${PATH_BIN}"; return 0;fi;if [[ -n "${PATH_LIB:-}" && -d "${PATH_LIB}" && -w "${PATH_LIB}" ]];then echo "${PATH_LIB}"; return 0;fi;if [[ -n "${BASH_LOADABLES_PATH:-}" ]];then local IFS=:;for _d in ${BASH_LOADABLES_PATH};do [[ -d "${_d}" && -w "${_d}" ]] && { echo "${_d}"; return 0; };done;fi;_d="${HOME}/.local/lib/bash";mkdir -p "${_d}" 2>/dev/null || true;if [[ -d "${_d}" && -w "${_d}" ]];then echo "${_d}"; return 0;fi;return 1;};argsh::builtin::download() { local _force="${1:-0}" _dir _dest _tag;if (( ! _force )) && argsh::builtin::try 2>/dev/null;then echo "argsh: builtins already installed" >&2;return 0;fi;[[ "$(uname -s)" == "Linux" && "$(uname -m)" == "x86_64" ]] || { echo "argsh: builtins only available for linux/amd64 (got $(uname -s)/$(uname -m))" >&2;return 1;};_dir="$(argsh::builtin::install_dir)" || { echo "argsh: no writable install path found for builtins" >&2;echo "  Run: argsh builtins install --path /your/writable/dir" >&2;return 1;};_dest="${_dir}/argsh.so";command -v curl &>/dev/null || { echo "argsh: curl is required to download builtins" >&2;return 1;};_tag="$(github::latest "arg-sh/argsh")" || { echo "argsh: failed to get latest release from GitHub" >&2;return 1;};echo "argsh: downloading argsh.so (${_tag})..." >&2;curl -fsSL -o "${_dest}" "https://github.com/arg-sh/argsh/releases/download/${_tag}/argsh.so" || { echo "argsh: download failed" >&2;rm -f "${_dest}";return 1;};echo "argsh: installed to ${_dest}" >&2;argsh::builtin::try "${_dest}" || { echo "argsh: downloaded file failed to load as builtin" >&2;rm -f "${_dest}";return 1;};return 0;};argsh::builtins() { case "${1:-}" in
install)shift; argsh::builtin::_install "${@}";;
update)shift; argsh::builtin::_install --force "${@}";;
*)local _loc;_loc="$(argsh::builtin::location 2>/dev/null)" || _loc="not installed";echo "argsh builtins: ${_loc}";echo "";echo "Usage: argsh builtins install|update [--force] [--path DIR]";;
esac;};argsh::builtin::_install() { local _force=0 _a49="";while [[ "${1:-}" == --* ]];do case "${1}" in
--force)_force=1; shift;;
--path)shift; _a49="${1}"; shift;;
*)echo "argsh: unknown option: ${1}" >&2; return 1;;
esac;done;if [[ -n "${_path}" ]];then [[ -d "${_path}" && -w "${_path}" ]] || { echo "argsh: directory not writable: ${_path}" >&2;return 1;};PATH_BIN="${_path}" argsh::builtin::download "${_force}";else argsh::builtin::download "${_force}";fi;};argsh::shebang() { local _argsh_builtin_mode="" _argsh_builtin_a49="";local -a _argsh_imports=();while [[ "${1:-}" == -* ]];do case "${1}" in
--builtin)_argsh_builtin_mode="required";shift;if [[ -n "${1:-}" && "${1:0:1}" != "-" && "${1}" == *.so ]];then _argsh_builtin_a49="${1}";shift;fi;;
--no-builtin)_argsh_builtin_mode="disabled";shift;;
--import|-i)shift;[[ -n "${1:-}" ]] || { echo "argsh: --import requires an argument" >&2; return 1; };_argsh_imports+=("${1}");shift;;
--version)echo "argsh ${ARGSH_VERSION:-unknown} (${ARGSH_COMMIT_SHA:-unknown})";return 0;;
--)shift; break;;
*)break;;
esac;done;local -r a42="${1}";: "${ARGSH_SOURCE="${a42}"}";export ARGSH_SOURCE;case "${a42}" in
builtins)shift; argsh::builtins "${@}"; return;;
esac;[[ "${BASH_SOURCE[-1]}" != "${a42}" && -f "${a42}" ]] || { binary::exists docker || { echo "This script requires Docker to be installed";return 1;} >&2;local a65="";! tty -s || a65="-it";docker run --rm ${a65} $(docker::user) -e "BATS_LOAD" -e "ARGSH_SOURCE" -e "GIT_COMMIT_SHA=$(git rev-parse HEAD 2>/dev/null || :)" -e "GIT_VERSION=$(git describe --tags --dirty 2>/dev/null || :)" ghcr.io/arg-sh/argsh:latest "${@}";return 0;};bash::version 4 3 0 || { echo "This script requires bash 4.3.0 or later";return 1;} >&2;declare -gi ARGSH_BUILTIN=0;case "${_argsh_builtin_mode}" in
required)argsh::builtin::try "${_argsh_builtin_path}" || { if [[ -n "${_argsh_builtin_path}" ]];then echo "argsh: builtin not found: ${_argsh_builtin_path}" >&2;return 1;fi;argsh::builtin::download || return 1;};ARGSH_BUILTIN=1;;
disabled);;
*)argsh::builtin::try && ARGSH_BUILTIN=1;;
esac;local _lib;for _lib in "${_argsh_imports[@]}";do import "${_lib}";done;shift;. "${a42}";};string::drop-index() { local a18="${1}";local a27="${2}";local a14="${3:-1}";echo "${a18:0:a27}${a18:a27+a14}";};string::random() { local a14="${1:-42}";local a23="${2:-"a-zA-Z0-9"}";local a63;until [[ "${a63:-}" =~ ^[[:alpha:]] ]];do a63=$(tr -dc "${a23}" < /dev/urandom | fold -w "${a14}" | head -n 1 || :);done;echo "${a63}";};string::indent() { local a18="${1:-'-'}";local a12="${2:-0}";local a46 a28;[[ ${a18} != '-' ]] || a18="$(cat)";mapfile -t a28 < <(echo "${a18}");for a46 in "${a28[@]}";do a46="$(string::trim-left "${a46}")";(( a12 == 0 )) || printf "%${a12}s" " ";echo "${a46}";done;};string::trim-left() { local a18="${1}";local a23="${2:-" "$'\n'$'\t'}";[[ -n ${a18:-} ]] || return 0;[[ ${a18} != '-' ]] || a18="$(cat)";while [[ -n "${a18}" ]];do [[ ${a23} == *"${a18:0:1}"* ]] || break;a18="${a18:1}";done;echo "${a18}";};string::trim-right() { local a18="${1:-'-'}";local a23="${2:-" "$'\n'$'\t'}";[[ ${a18} != '-' ]] || a18="$(cat)";while [[ -n "${a18}" ]];do [[ ${a23} == *"${a18: -1}"* ]] || break;a18="${a18:0: -1}";done;echo "${a18}";};string::trim() { local a18="${1:-'-'}";local a23="${2:-" "$'\n'$'\t'}";[[ ${a18} != '-' ]] || a18="$(cat)";echo "${a18}" | string::trim-left - "${a23}" | string::trim-right - "${a23}";};to::string() { local a36="${1}";echo "${a36}";};to::boolean() { local a36="${1}";case "${a36}" in
""|"false"|"0")a36="0";;
*)a36="1";;
esac;echo "${a36}";};to::int() { local a36="${1}";[[ ${a36} =~ ^-?[0-9]+$ ]] || return 1;echo "${a36}";};to::float() { local a36="${1}";[[ ${a36} =~ ^-?[0-9]+(\.[0-9]+)?$ ]] || return 1;echo "${a36}";};to::stdin() { local a36="${1}";[[ ${a36} != "-" ]] || a36="$(cat)";echo "${a36}";};to::file() { local a36="${1}";[[ -f "${a36}" ]] || return 1;echo "${a36}";};[[ "${BASH_SOURCE[0]}" != "${0}" ]] || argsh::shebang "${@}"